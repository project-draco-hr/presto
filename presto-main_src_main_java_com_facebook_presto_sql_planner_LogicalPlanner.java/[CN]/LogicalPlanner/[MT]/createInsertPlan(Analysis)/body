{
  TableHandle target=analysis.getInsertTarget().get();
  TableMetadata tableMetadata=metadata.getTableMetadata(session,target);
  return createTableWriterPlan(analysis,createRelationPlan(analysis),new InsertReference(target),getVisibleColumnNames(tableMetadata));
}
