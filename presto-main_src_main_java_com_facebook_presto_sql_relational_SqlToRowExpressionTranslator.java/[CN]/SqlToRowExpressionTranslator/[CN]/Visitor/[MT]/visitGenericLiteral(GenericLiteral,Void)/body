{
  Type type=metadata.getType(node.getType());
  if (type == null) {
    throw new IllegalArgumentException("Unsupported type: " + node.getType());
  }
  return call(castSignature(types.get(node),VARCHAR),constant(Slices.copiedBuffer(node.getValue(),StandardCharsets.UTF_8),VARCHAR));
}
