{
  if (cursor.isNull(field)) {
    return currentMin;
  }
  Slice value=cursor.getSlice(field);
  if (currentMin == null) {
    return value;
  }
 else {
    return Ordering.natural().min(currentMin,value);
  }
}
