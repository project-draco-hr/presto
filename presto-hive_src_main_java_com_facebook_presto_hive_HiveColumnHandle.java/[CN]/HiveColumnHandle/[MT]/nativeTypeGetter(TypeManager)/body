{
  return new Function<HiveColumnHandle,Type>(){
    @Override public Type apply(    HiveColumnHandle input){
      return typeManager.getType(parseTypeSignature(input.getTypeName()));
    }
  }
;
}
