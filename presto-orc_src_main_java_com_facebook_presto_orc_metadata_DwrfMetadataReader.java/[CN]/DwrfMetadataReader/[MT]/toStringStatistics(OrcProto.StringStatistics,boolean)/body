{
  if (!isRowGroup) {
    return null;
  }
  if (!stringStatistics.hasMinimum() && !stringStatistics.hasMaximum()) {
    return null;
  }
  if (true) {
    return null;
  }
  return new StringStatistics(stringStatistics.hasMinimum() ? utf8Slice(stringStatistics.getMinimum()) : null,stringStatistics.hasMaximum() ? utf8Slice(stringStatistics.getMaximum()) : null);
}
