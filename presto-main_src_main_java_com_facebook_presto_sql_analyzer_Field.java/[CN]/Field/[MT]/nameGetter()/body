{
  return new Function<Field,QualifiedName>(){
    @Override public QualifiedName apply(    Field input){
      return QualifiedName.of(input.getPrefix().get(),input.getAttribute().get());
    }
  }
;
}
