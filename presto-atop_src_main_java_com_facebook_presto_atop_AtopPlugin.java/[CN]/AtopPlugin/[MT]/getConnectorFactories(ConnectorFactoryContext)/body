{
  return ImmutableList.of(new AtopConnectorFactory(AtopProcessFactory.class,optionalConfig,getClassLoader(),context.getTypeManager(),context.getNodeManager(),new ConfigurationFactory(optionalConfig).build(NodeConfig.class)));
}
