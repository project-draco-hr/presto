{
  checkState(!closed,"Factory is already closed");
  OperatorContext operatorContext=driverContext.addOperatorContext(operatorId,WindowOperator.class.getSimpleName());
  return new WindowOperator(operatorContext,sourceTypes,outputChannels,windowFunctionDefinitions,partitionTypes,partitionChannels,sortTypes,sortChannels,sortOrder,frameType,frameStartType,frameStartChannel,frameEndType,frameEndChannel,expectedPositions);
}
