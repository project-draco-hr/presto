{
  return new WindowOperatorFactory(operatorId,sourceTypes,outputChannels,windowFunctionDefinitions,partitionChannels,preGroupedChannels,sortChannels,sortOrder,preSortedChannelPrefix,frameInfo,expectedPositions);
}
