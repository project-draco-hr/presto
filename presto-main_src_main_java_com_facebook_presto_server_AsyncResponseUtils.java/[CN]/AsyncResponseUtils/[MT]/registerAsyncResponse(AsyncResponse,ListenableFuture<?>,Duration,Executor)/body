{
  Response timeoutResponse=Response.status(Status.SERVICE_UNAVAILABLE).entity("Timed out after waiting for " + timeout.convertToMostSuccinctTimeUnit()).build();
  registerAsyncResponse(asyncResponse,futureResponse,timeout,executor,timeoutResponse);
}
