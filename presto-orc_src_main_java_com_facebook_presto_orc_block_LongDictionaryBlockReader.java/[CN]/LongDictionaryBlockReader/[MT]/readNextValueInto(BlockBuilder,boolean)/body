{
  if (presentStream != null && !presentStream.nextBit()) {
    if (!skipNull) {
      builder.appendNull();
      return true;
    }
    return false;
  }
  BIGINT.writeLong(builder,nextValue());
  return true;
}
