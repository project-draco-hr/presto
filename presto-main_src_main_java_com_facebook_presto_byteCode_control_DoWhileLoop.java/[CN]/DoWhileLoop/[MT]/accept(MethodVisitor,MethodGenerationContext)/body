{
  Block block=new Block().visitLabel(beginLabel).append(body).visitLabel(continueLabel).append(condition).ifZeroGoto(endLabel).gotoLabel(beginLabel).visitLabel(endLabel);
  block.accept(visitor,generationContext);
}
