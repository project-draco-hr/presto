{
  Block block=new Block(context).visitLabel(beginLabel).append(body).visitLabel(continueLabel).append(condition).ifZeroGoto(endLabel).gotoLabel(beginLabel).visitLabel(endLabel);
  block.accept(visitor);
}
