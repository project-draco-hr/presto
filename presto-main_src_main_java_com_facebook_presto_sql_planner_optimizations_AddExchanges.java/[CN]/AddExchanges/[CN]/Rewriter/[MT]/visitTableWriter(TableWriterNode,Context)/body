{
  PlanWithProperties source=node.getSource().accept(this,context);
  if (redistributeWrites) {
    source=withDerivedProperties(partitionedExchange(idAllocator.getNextId(),source.getNode(),new PartitionFunctionBinding(ROUND_ROBIN,ImmutableList.of())),source.getProperties());
  }
  return rebaseAndDeriveProperties(node,source);
}
