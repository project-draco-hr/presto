{
  if (this == o) {
    return true;
  }
  if (!(o instanceof TpchSplit)) {
    return false;
  }
  TpchSplit tpchSplit=(TpchSplit)o;
  if (tableHandle.equals(tpchSplit.tableHandle) && partNumber == tpchSplit.partNumber && totalParts == tpchSplit.totalParts) {
    return true;
  }
  return false;
}
