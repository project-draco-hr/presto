{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof JoinHashCacheKey)) {
    return false;
  }
  final JoinHashCacheKey other=(JoinHashCacheKey)obj;
  return Objects.equal(this.types,other.types) && Objects.equal(this.hashChannels,other.hashChannels);
}
