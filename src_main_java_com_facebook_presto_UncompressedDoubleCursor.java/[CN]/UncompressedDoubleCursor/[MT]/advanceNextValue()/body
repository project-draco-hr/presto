{
  if (block == null) {
    return false;
  }
  if (index < 0) {
    index=0;
    offset=0;
    return true;
  }
 else   if (index < block.getCount() - 1) {
    index++;
    offset+=SIZE_OF_DOUBLE;
    return true;
  }
 else   if (iterator.hasNext()) {
    block=iterator.next();
    index=0;
    offset=0;
    return true;
  }
 else {
    block=null;
    index=-1;
    offset=-1;
    return false;
  }
}
