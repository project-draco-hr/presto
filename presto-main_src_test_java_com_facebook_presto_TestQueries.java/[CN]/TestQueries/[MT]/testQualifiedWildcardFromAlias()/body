{
  List<Tuple> expected=computeExpected("SELECT orderkey, custkey, orderstatus, totalprice, orderdate, orderpriority, clerk, shippriority, comment FROM ORDERS",FIXED_INT_64,FIXED_INT_64,VARIABLE_BINARY,DOUBLE,VARIABLE_BINARY,VARIABLE_BINARY,VARIABLE_BINARY,VARIABLE_BINARY,VARIABLE_BINARY);
  List<Tuple> actual=computeActual("SELECT T.* FROM ORDERS T");
  assertEqualsIgnoreOrder(actual,expected);
}
