{
  Preconditions.checkNotNull(session,"session is null");
  return new Function<TupleStream,Cursor>(){
    @Override public Cursor apply(    TupleStream input){
      return input.cursor(session);
    }
  }
;
}
