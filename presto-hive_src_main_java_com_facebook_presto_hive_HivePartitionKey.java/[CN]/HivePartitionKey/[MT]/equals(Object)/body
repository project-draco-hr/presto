{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  HivePartitionKey other=(HivePartitionKey)obj;
  return Objects.equal(this.name,other.name) && Objects.equal(this.hiveType,other.hiveType) && Objects.equal(this.value,other.value);
}
