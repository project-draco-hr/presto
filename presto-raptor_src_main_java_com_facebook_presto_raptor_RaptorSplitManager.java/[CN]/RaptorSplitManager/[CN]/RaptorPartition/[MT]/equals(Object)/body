{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  RaptorPartition other=(RaptorPartition)obj;
  return this.partitionId == other.partitionId && Objects.equal(this.tupleDomain,other.tupleDomain);
}
