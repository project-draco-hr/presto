{
  while (source.hasNext()) {
    Optional<PositionBlock> block=source.next().selectPositions(predicate);
    if (block.isPresent()) {
      return block.get();
    }
  }
  endOfData();
  return null;
}
