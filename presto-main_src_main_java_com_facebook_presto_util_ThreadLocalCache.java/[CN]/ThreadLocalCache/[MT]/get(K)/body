{
  LinkedHashMap<K,V> map=cache.get();
  V value=map.get(key);
  if (value != null) {
    return value;
  }
  value=load(key);
  requireNonNull(value,"value must not be null");
  map.put(key,value);
  return value;
}
