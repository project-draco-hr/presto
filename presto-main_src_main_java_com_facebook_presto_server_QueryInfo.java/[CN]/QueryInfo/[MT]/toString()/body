{
  return Objects.toStringHelper(this).add("queryId",queryId).add("tupleInfos",tupleInfos).add("state",state).add("bufferedPages",bufferedPages).add("splits",splits).add("completedSplits",completedSplits).add("stages",stages).toString();
}
