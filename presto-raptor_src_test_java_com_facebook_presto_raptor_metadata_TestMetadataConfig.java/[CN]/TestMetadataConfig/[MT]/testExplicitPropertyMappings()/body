{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("raptor.startup-grace-period","42m").build();
  MetadataConfig expected=new MetadataConfig().setStartupGracePeriod(new Duration(42,MINUTES));
  assertFullMapping(properties,expected);
}
