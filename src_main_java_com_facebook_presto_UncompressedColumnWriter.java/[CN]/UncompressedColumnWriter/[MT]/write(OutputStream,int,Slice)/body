{
  Slice header=Slices.allocate(SIZE_OF_INT + SIZE_OF_INT);
  header.output().appendInt(slice.length()).appendInt(tupleCount);
  header.getBytes(0,out,header.length());
  slice.getBytes(0,out,slice.length());
}
