{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  final BufferResult other=(BufferResult)obj;
  return Objects.equals(this.token,other.token) && Objects.equals(this.nextToken,other.nextToken) && Objects.equals(this.bufferClosed,other.bufferClosed)&& Objects.equals(this.pages,other.pages)&& Objects.equals(this.partitionFunction,other.partitionFunction);
}
