{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  final BufferResult other=(BufferResult)obj;
  return Objects.equal(this.token,other.token) && Objects.equal(this.nextToken,other.nextToken) && Objects.equal(this.bufferClosed,other.bufferClosed)&& Objects.equal(this.pages,other.pages)&& Objects.equal(this.partitionFunction,other.partitionFunction);
}
