{
  if (node.getColumnNames() != null && !node.getColumnNames().isEmpty()) {
    throw new UnsupportedOperationException("not yet implemented: relation alias with column mappings");
  }
  process(node.getRelation(),indent);
  builder.append(' ').append(node.getAlias());
  return null;
}
