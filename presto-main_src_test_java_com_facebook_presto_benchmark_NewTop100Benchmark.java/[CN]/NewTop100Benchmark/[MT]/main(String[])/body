{
  ExecutorService executor=newCachedThreadPool(daemonThreadsNamed("test"));
  new NewTop100Benchmark(executor,DEFAULT_TPCH_BLOCKS_PROVIDER).runBenchmark(new SimpleLineBenchmarkResultWriter(System.out));
}
