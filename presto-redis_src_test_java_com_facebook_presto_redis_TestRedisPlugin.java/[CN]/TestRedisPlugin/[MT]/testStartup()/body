{
  ConnectorFactory factory=testConnectorExists();
  Connector c=factory.create("test-connector",ImmutableMap.<String,String>builder().put("redis.table-names","test").put("redis.nodes","localhost:6379").build(),new ConnectorContext(){
  }
);
  assertNotNull(c);
}
