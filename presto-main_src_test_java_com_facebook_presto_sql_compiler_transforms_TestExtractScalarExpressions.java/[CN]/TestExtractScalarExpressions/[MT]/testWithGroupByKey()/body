{
  assertRewrite("SELECT ORDERS.orderkey key, SUM(orderkey + custkey) sum\n" + "FROM ORDERS\n" + "GROUP BY ORDERS.orderkey","SELECT _R0._a0 key, SUM(_R0._a1) sum\n" + "FROM (\n" + "   SELECT ORDERS.orderkey _a0, (orderkey + custkey) _a1\n"+ "   FROM ORDERS\n"+ ") _R0\n"+ "GROUP BY _R0._a0");
}
