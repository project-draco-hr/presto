{
  assertRewrite("SELECT SUM(ORDERS.orderkey + ORDERS.custkey) x FROM ORDERS","SELECT SUM(_R0._a0) x\n" + "FROM (\n" + "   SELECT ORDERS.orderkey + ORDERS.custkey _a0\n"+ "   FROM ORDERS\n"+ ") _R0");
}
