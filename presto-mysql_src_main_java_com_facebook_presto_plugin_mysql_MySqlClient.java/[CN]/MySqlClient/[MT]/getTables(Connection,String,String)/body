{
  DatabaseMetaData metadata=connection.getMetaData();
  String escape=metadata.getSearchStringEscape();
  return metadata.getTables(escapeNamePattern(schemaName,escape),null,escapeNamePattern(tableName,escape),new String[]{"TABLE"});
}
