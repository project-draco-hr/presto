{
  List<FunctionInfo> functions=new FunctionRegistry.FunctionListBuilder().scalar(CustomAdd.class).build();
  FunctionRegistry registry=new FunctionRegistry(true);
  registry.addFunctions(functions);
  registry.addFunctions(functions);
}
