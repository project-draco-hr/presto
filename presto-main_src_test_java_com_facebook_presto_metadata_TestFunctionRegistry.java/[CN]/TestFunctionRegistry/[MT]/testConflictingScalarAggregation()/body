{
  List<FunctionInfo> functions=new FunctionRegistry.FunctionListBuilder().scalar(ScalarSum.class).build();
  FunctionRegistry registry=new FunctionRegistry(true);
  registry.addFunctions(functions);
}
