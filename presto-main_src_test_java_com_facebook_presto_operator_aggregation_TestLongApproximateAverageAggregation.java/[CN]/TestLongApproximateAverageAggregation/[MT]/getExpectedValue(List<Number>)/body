{
  int length=0;
  long sum=0;
  for (  Number value : values) {
    if (value == null) {
      continue;
    }
    length++;
    sum+=value.longValue();
  }
  if (length == 0) {
    return null;
  }
  return (double)sum / length;
}
