{
  return new CsvColumnProcessor(){
    @Override public void process(    String value,    RowSourceBuilder.RowBuilder rowBuilder){
      rowBuilder.append((long)Double.parseDouble(value));
    }
  }
;
}
