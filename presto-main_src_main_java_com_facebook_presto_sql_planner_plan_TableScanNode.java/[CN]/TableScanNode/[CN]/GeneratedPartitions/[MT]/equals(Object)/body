{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  GeneratedPartitions other=(GeneratedPartitions)obj;
  return Objects.equals(this.tupleDomainInput,other.tupleDomainInput) && Objects.equals(this.partitions,other.partitions);
}
