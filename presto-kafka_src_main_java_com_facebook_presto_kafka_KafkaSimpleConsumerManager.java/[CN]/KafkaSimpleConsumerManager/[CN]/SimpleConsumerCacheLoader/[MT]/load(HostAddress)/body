{
  log.info("Creating new Consumer for %s",host);
  return new SimpleConsumer(host.getHostText(),host.getPort(),Ints.checkedCast(kafkaConnectorConfig.getKafkaConnectTimeout().toMillis()),Ints.checkedCast(kafkaConnectorConfig.getKafkaBufferSize().toBytes()),format("presto-kafka-%s-%s",connectorId,nodeManager.getCurrentNode().getNodeIdentifier()));
}
