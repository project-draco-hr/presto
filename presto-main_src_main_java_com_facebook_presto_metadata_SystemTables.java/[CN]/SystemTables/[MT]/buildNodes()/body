{
  InternalTable.Builder table=InternalTable.builder(METADATA.get(TABLE_NODES));
  for (  Node node : nodeManager.getActiveNodes()) {
    table.add(table.getTupleInfo().builder().append(node.getNodeIdentifier()).append(node.getHttpUri().toString()).append("YES").build());
  }
  return table.build();
}
