{
  if (enabled) {
    if (started.compareAndSet(false,true)) {
      this.scheduledFuture.set(executorService.scheduleAtFixedRate(new ShardCleanerRunnable(),(long)interval.toMillis(),(long)interval.toMillis(),TimeUnit.MILLISECONDS));
    }
  }
}
