{
  testRoundTripType(createHiveStructInspector(objectInspector),transform(writeValues,toHiveStruct()),transform(readJsonValues,toJsonStruct()));
  if (structuralNullTestsEnabled) {
    testRoundTripType(createHiveStructInspector(objectInspector),transform(insertNullEvery(5,writeValues),toHiveStruct()),transform(insertNullEvery(5,readJsonValues),toJsonStruct()));
    testRoundTripType(createHiveStructInspector(objectInspector),transform(transform(writeValues,constant(null)),toHiveStruct()),transform(transform(writeValues,constant(null)),toJsonStruct()));
  }
}
