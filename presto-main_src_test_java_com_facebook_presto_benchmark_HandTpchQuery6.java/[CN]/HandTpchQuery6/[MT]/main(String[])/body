{
  ExecutorService executor=newCachedThreadPool(daemonThreadsNamed("test"));
  new HandTpchQuery6(executor,DEFAULT_TPCH_BLOCKS_PROVIDER).runBenchmark(new SimpleLineBenchmarkResultWriter(System.out));
}
