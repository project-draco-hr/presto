{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("analyzer.experimental-syntax-enabled","true").build();
  AnalyzerConfig expected=new AnalyzerConfig().setExperimentalSyntaxEnabled(true);
  assertFullMapping(properties,expected);
}
