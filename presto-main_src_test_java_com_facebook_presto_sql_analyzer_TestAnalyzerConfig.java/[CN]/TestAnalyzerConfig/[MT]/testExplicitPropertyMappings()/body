{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("analyzer.approximate-queries-enabled","true").build();
  AnalyzerConfig expected=new AnalyzerConfig().setApproximateQueriesEnabled(true);
  assertFullMapping(properties,expected);
}
