{
  if (failure == null) {
    return null;
  }
  return new FailureInfo(failure.getClass().getCanonicalName(),failure.getMessage(),toFailure(failure.getCause()),toFailures(asList(failure.getSuppressed())),Lists.transform(asList(failure.getStackTrace()),toStringFunction()),getErrorLocation(failure));
}
