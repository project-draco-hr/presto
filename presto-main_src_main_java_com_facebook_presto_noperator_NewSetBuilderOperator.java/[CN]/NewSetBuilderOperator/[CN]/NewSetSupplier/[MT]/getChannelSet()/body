{
  return Futures.transform(channelSetFuture,new Function<ChannelSet,ChannelSet>(){
    @Override public ChannelSet apply(    ChannelSet channelSet){
      return new ChannelSet(channelSet);
    }
  }
);
}
