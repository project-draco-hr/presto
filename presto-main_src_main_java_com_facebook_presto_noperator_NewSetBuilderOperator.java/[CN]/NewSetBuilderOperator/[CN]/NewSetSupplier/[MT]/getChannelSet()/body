{
  return Futures.transform(channelSetFuture,new Function<NewChannelSet,NewChannelSet>(){
    @Override public NewChannelSet apply(    NewChannelSet channelSet){
      return new NewChannelSet(channelSet);
    }
  }
);
}
