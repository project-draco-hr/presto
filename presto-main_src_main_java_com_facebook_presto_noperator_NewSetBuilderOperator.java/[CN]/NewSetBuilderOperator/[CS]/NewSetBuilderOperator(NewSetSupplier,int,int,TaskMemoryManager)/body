{
  this.setSupplier=checkNotNull(setSupplier,"setProvider is null");
  this.setChannel=setChannel;
  this.channelSetBuilder=new ChannelSetBuilder(setSupplier.getTupleInfo(),expectedPositions,checkNotNull(taskMemoryManager,"taskMemoryManager is null"));
}
