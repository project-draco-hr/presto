{
  return new Predicate<ComparisonExpression>(){
    @Override public boolean apply(    ComparisonExpression expression){
      return expression.getType() == type && left.isAssignableFrom(expression.getLeft().getClass()) && right.isAssignableFrom(expression.getRight().getClass());
    }
  }
;
}
