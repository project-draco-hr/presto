{
switch (type) {
case BOOLEAN:
    return BOOLEAN;
case DOUBLE:
  return DOUBLE;
case LONG:
return BIGINT;
case STRING:
return VARCHAR;
default :
throw new IllegalStateException("Unknown type " + type);
}
}
