{
  try {
    new SharedBuffer<>(0);
    fail("Expected IllegalStateException");
  }
 catch (  IllegalArgumentException e) {
  }
  try {
    new SharedBuffer<>(-1);
    fail("Expected IllegalStateException");
  }
 catch (  IllegalArgumentException e) {
  }
}
