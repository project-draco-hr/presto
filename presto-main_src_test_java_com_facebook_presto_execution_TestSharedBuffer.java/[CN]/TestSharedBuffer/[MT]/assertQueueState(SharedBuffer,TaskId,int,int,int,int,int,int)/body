{
  assertEquals(getBufferInfo(sharedBuffer,queueId),new BufferInfo(queueId,false,bufferedPages,pagesSent,new PageBufferInfo(partition,pageBufferBufferedPages,pageBufferQueuedPages,sizeOfPages(pageBufferBufferedPages).toBytes(),pageBufferPagesSent)));
}
