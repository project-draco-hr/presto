{
  return ImmutableList.copyOf(transform(masterQueries.values(),new Function<MasterQueryState,QueryInfo>(){
    @Override public QueryInfo apply(    MasterQueryState masterQueryState){
      return masterQueryState.toQueryInfo();
    }
  }
));
}
