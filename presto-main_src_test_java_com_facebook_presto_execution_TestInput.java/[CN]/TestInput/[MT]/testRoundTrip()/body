{
  Input expected=new Input("connectorId","schema","table",ImmutableList.of(new Column("column1","string",Optional.empty()),new Column("column2","string",Optional.empty()),new Column("column3","string",Optional.empty())));
  String json=codec.toJson(expected);
  Input actual=codec.fromJson(json);
  assertEquals(actual,expected);
}
