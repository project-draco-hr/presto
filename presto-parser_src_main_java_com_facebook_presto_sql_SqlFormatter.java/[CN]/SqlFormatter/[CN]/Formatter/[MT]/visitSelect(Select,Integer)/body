{
  append(indent,"SELECT ").append(node.isDistinct() ? "DISTINCT " : "").append(Joiner.on(", ").join(Iterables.transform(node.getSelectItems(),expressionFormatterFunction()))).append('\n');
  return null;
}
