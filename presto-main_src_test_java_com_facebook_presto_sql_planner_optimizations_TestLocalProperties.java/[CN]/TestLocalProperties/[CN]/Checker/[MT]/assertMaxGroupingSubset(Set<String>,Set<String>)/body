{
  Set<String> actual=LocalProperty.getMaxGroupingSubset(properties,constants,candidates);
  assertTrue(actual.equals(expected),String.format("Expected %s => %s given constants = %s, candidates = %s. However, found => %s",properties,new GroupingProperty<>(expected),constants,candidates,new GroupingProperty<>(actual)));
  return this;
}
