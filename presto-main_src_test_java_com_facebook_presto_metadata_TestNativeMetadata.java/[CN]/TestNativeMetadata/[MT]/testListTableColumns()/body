{
  metadata.createTable(getOrdersTable());
  Map<QualifiedTableName,List<ColumnMetadata>> columns=metadata.listTableColumns(QualifiedTablePrefix.builder("default").build());
  assertEquals(columns,ImmutableMap.of(DEFAULT_TEST_ORDERS,columnsBuilder().column("orderkey",FIXED_INT_64).column("custkey",FIXED_INT_64).column("totalprice",DOUBLE).column("orderdate",VARIABLE_BINARY).build()));
}
