{
  if (this == obj) {
    return true;
  }
  if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  Statement o=(Statement)obj;
  return Objects.equal(statement,o.statement) && Objects.equal(terminator,o.terminator);
}
