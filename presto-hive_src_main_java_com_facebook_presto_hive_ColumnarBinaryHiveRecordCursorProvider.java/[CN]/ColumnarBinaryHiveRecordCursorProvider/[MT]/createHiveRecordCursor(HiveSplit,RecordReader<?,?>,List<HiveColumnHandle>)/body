{
  if (usesColumnarBinarySerDe(split)) {
    return Optional.<RecordCursor>of(new ColumnarBinaryHiveRecordCursor<>(bytesRecordReader(recordReader),split.getLength(),split.getSchema(),split.getPartitionKeys(),columns));
  }
  return Optional.absent();
}
