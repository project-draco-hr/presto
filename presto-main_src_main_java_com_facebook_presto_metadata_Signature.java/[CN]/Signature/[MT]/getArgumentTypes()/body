{
  return FluentIterable.from(argumentTypes).transform(new Function<TypeSignature,String>(){
    @Override public String apply(    TypeSignature input){
      return input.toString();
    }
  }
).toList();
}
