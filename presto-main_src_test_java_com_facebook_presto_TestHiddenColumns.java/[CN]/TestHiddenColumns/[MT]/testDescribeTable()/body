{
  MaterializedResult expected=MaterializedResult.resultBuilder(SESSION,VARCHAR,VARCHAR,BOOLEAN,BOOLEAN,VARCHAR).row("regionkey","bigint",true,false,null).row("name","varchar",true,false,null).row("comment","varchar",true,false,null).build();
  assertEquals(runner.execute("DESC REGION"),expected);
}
