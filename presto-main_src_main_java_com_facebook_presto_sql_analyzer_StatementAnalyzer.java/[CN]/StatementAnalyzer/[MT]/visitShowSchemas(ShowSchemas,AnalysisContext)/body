{
  Query query=new Query(Optional.empty(),new QuerySpecification(selectList(aliasedName("schema_name","Schema")),table(QualifiedName.of(node.getCatalog().orElse(session.getCatalog()),TABLE_SCHEMATA.getSchemaName(),TABLE_SCHEMATA.getTableName())),Optional.empty(),ImmutableList.<Expression>of(),Optional.empty(),ImmutableList.of(ascending("schema_name")),Optional.empty()),ImmutableList.<SortItem>of(),Optional.empty(),Optional.empty());
  return process(query,context);
}
