{
  Query query=new Query(Optional.<With>absent(),new QuerySpecification(selectList(aliasedName("catalog_name","Catalog")),table(QualifiedName.of(session.getCatalog(),CATALOG_TABLE_NAME.getSchemaName(),CATALOG_TABLE_NAME.getTableName())),Optional.<Expression>absent(),ImmutableList.<Expression>of(),Optional.<Expression>absent(),ImmutableList.of(ascending("catalog_name")),Optional.<String>absent()),ImmutableList.<SortItem>of(),Optional.<String>absent(),Optional.<Approximate>absent());
  return process(query,context);
}
