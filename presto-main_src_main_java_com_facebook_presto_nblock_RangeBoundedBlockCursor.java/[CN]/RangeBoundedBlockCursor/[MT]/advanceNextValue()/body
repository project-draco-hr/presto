{
  if (isFinished()) {
    return false;
  }
  if (!initialized) {
    initializeUnderlyingCursor();
  }
 else {
    cursor.advanceToPosition(getCurrentValueEndPosition() + 1);
  }
  return processResult();
}
