{
  checkCatalogName(catalogName);
  return new Predicate<List<String>>(){
    @Override public boolean apply(    List<String> key){
      return key.equals(ImmutableList.of(catalogName));
    }
  }
;
}
