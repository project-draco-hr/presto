{
  if (rows.hasNext()) {
    return rows.next();
  }
  rowBuffer=new ArrayList<>();
  if (pages.hasNext()) {
    pages.next();
    rows=rowBuffer.iterator();
    return computeNext();
  }
  return endOfData();
}
