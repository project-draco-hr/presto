{
  return node.getName() + "(" + Joiner.on(", ").join(Iterables.transform(node.getArguments(),new Function<Expression,Object>(){
    @Override public Object apply(    Expression input){
      return process(input,null);
    }
  }
))+ ")";
}
