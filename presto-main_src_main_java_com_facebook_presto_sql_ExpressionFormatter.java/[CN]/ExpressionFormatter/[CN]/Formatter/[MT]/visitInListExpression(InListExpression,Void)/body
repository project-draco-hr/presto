{
  return "(" + Joiner.on(", ").join(Iterables.transform(node.getValues(),new Function<Expression,String>(){
    @Override public String apply(    Expression input){
      return process(input,null);
    }
  }
)) + ")";
}
