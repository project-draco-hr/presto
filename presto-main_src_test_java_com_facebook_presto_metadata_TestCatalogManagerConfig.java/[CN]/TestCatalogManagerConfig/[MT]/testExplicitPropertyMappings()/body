{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("catalog.config-dir","/foo").build();
  CatalogManagerConfig expected=new CatalogManagerConfig().setCatalogConfigurationDir(new File("/foo"));
  assertFullMapping(properties,expected);
}
