{
  return Objects.toStringHelper(this).add("session",session).add("queryId",queryId).add("stageId",stageId).add("fragment",fragment).add("initialSources",initialSources).add("initialOutputIds",initialOutputIds).toString();
}
