{
  return Objects.toStringHelper(this).add("session",session).add("queryId",queryId).add("stageId",stageId).add("fragment",fragment).add("fixedSources",fixedSources).add("initialOutputIds",initialOutputIds).toString();
}
