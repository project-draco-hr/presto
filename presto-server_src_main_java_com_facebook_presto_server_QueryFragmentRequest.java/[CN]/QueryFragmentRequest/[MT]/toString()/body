{
  return Objects.toStringHelper(this).add("session",session).add("queryId",queryId).add("stageId",stageId).add("fragment",fragment).add("exchangeSources",exchangeSources).add("outputIds",outputIds).toString();
}
