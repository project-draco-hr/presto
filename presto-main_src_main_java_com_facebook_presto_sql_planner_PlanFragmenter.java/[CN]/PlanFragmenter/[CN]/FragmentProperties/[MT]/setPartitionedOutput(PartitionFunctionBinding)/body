{
  checkState(!this.partitionFunction.isPresent() || this.partitionFunction.get().equals(partitionFunction),"Cannot overwrite output partitioning with %s (currently set to %s)",partitionFunction,this.partitionFunction);
  this.partitionFunction=Optional.of(partitionFunction);
  return this;
}
