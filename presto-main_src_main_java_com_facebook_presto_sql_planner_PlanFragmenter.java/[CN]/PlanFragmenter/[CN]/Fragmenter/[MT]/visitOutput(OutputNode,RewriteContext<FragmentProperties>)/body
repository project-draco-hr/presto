{
  context.get().setSingleNodeDistribution().setOutputLayout(node.getOutputSymbols()).setPartitionedOutput(new PartitionFunctionBinding(SINGLE_DISTRIBUTION,ImmutableList.of()));
  return context.defaultRewrite(node,context.get());
}
