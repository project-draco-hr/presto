{
  assertQuery("SELECT linenumber, suppkey, SUM(quantity) " + "FROM lineitem " + "GROUP BY GROUPING SETS ((linenumber, suppkey)) ","SELECT linenumber, suppkey, SUM(quantity) " + "FROM lineitem " + "GROUP BY linenumber, suppkey");
}
