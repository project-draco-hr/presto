{
  assertQuery("SELECT linenumber, suppkey, SUM(quantity) FROM lineitem GROUP BY GROUPING SETS ((linenumber, suppkey), (suppkey))","SELECT linenumber, suppkey, SUM(quantity) FROM lineitem GROUP BY linenumber, suppkey UNION " + "SELECT NULL, suppkey, SUM(quantity) FROM lineitem GROUP BY suppkey");
}
