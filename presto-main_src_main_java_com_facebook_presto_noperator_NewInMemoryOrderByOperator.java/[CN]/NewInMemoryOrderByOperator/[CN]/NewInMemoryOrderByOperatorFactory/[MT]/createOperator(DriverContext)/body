{
  checkState(!closed,"Factory is already closed");
  OperatorContext operatorContext=driverContext.addOperatorContext(operatorId,NewInMemoryOrderByOperator.class.getSimpleName());
  return new NewInMemoryOrderByOperator(operatorContext,sourceTupleInfos,orderByChannel,outputChannels,expectedPositions,sortFields,sortOrder);
}
