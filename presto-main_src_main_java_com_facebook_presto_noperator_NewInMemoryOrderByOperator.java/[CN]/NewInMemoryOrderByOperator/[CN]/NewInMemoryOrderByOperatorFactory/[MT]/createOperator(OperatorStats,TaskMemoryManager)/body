{
  checkState(!closed,"Factory is already closed");
  return new NewInMemoryOrderByOperator(sourceTupleInfos,orderByChannel,outputChannels,expectedPositions,sortFields,sortOrder,taskMemoryManager);
}
