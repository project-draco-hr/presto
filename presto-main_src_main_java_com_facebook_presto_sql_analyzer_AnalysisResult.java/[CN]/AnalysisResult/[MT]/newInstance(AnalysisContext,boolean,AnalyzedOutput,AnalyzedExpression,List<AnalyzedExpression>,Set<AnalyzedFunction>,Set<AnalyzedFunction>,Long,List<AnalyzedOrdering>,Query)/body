{
  return new AnalysisResult(context.getSymbolAllocator(),context.getTableDescriptors(),context.getTableMetadata(),context.getInlineViews(),context.getJoinCriteria(),distinct,aggregations,windowsFunctions,predicate,output,groupBy,orderBy,limit,rewrittenQuery);
}
