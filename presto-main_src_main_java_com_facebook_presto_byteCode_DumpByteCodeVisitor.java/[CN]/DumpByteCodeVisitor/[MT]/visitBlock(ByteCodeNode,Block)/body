{
  if (block.getDescription() != null) {
    line().add(block.getDescription()).add("{").print();
  }
 else {
    printLine("{");
  }
  indentLevel++;
  visitBlockContents(block);
  indentLevel--;
  printLine("}");
  return null;
}
