{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("backup.provider","file").put("backup.timeout","42s").build();
  BackupConfig expected=new BackupConfig().setProvider("file").setTimeout(new Duration(42,SECONDS));
  assertFullMapping(properties,expected);
}
