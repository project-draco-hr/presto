{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("backup.provider","file").build();
  BackupConfig expected=new BackupConfig().setProvider("file");
  assertFullMapping(properties,expected);
}
