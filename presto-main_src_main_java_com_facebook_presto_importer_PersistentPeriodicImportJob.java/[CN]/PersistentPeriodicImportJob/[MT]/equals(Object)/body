{
  if (object == this) {
    return true;
  }
 else   if (object == null || (getClass() != object.getClass())) {
    return false;
  }
  PersistentPeriodicImportJob that=(PersistentPeriodicImportJob)object;
  return Objects.equal(this.importJob,that.importJob) && this.jobId == that.jobId;
}
