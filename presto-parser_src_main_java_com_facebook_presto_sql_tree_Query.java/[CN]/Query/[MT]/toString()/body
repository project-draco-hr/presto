{
  return toStringHelper(this).add("with",with.orElse(null)).add("queryBody",queryBody).add("orderBy",orderBy).add("limit",limit.orElse(null)).add("approximate",approximate.orElse(null)).omitNullValues().toString();
}
