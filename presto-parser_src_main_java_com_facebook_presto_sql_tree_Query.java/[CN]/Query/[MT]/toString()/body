{
  return Objects.toStringHelper(this).add("with",with.orNull()).add("select",select).add("from",from).add("where",where.orNull()).add("groupBy",groupBy).add("having",having.orNull()).add("orderBy",orderBy).add("limit",limit.orNull()).omitNullValues().toString();
}
