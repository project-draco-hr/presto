{
  return new Function<Timestamp,String>(){
    @Nullable @Override public String apply(    @Nullable Timestamp input){
      if (input == null) {
        return null;
      }
      return TIMESTAMP_FORMATTER.print(input.getTime());
    }
  }
;
}
