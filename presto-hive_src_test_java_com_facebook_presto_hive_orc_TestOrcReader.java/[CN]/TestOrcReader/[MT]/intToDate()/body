{
  return new Function<Integer,Date>(){
    @Nullable @Override public Date apply(    @Nullable Integer input){
      if (input == null) {
        return null;
      }
      Date date=new Date(0);
      date.setTime(TimeUnit.DAYS.toMillis(input));
      return date;
    }
  }
;
}
