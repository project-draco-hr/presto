{
  return new Function<Timestamp,Long>(){
    @Nullable @Override public Long apply(    @Nullable Timestamp input){
      if (input == null) {
        return null;
      }
      return input.getTime();
    }
  }
;
}
