{
  String state;
  if (closed) {
    state="closed";
  }
 else   if (future != null) {
    state="running";
  }
 else {
    state="queued";
  }
  return new ExchangeClientStatus(location,state,lastUpdate);
}
