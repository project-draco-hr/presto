{
  return new Function<NamedParameterDefinition,String>(){
    @Override public String apply(    NamedParameterDefinition input){
      return input.getType().getJavaClassName() + " " + input.getName();
    }
  }
;
}
