{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("import.enabled","false").put("query.operator.max-memory","1GB").build();
  QueryManagerConfig expected=new QueryManagerConfig().setMaxOperatorMemoryUsage(new DataSize(1,Unit.GIGABYTE)).setImportsEnabled(false);
  ConfigAssertions.assertFullMapping(properties,expected);
}
