{
  if (this == obj) {
    return true;
  }
 else   if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  DropAlias o=(DropAlias)obj;
  return Objects.equal(remote,o.remote);
}
