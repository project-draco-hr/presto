{
  while (true) {
    List<Column> columns=client.current().getColumns();
    if (columns != null) {
      return columns;
    }
    if (!client.hasNext()) {
      throw internalServerError("No columns");
    }
    client.next();
  }
}
