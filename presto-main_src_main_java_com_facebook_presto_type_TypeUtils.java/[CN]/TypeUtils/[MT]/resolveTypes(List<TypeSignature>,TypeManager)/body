{
  return FluentIterable.from(typeNames).transform(new Function<TypeSignature,Type>(){
    @Override public Type apply(    TypeSignature type){
      return checkNotNull(typeManager.getType(type),"Type '%s' not found",type);
    }
  }
).toList();
}
