{
  return ImmutableList.copyOf(Iterables.transform(inetAddresses,new Function<InetAddress,HostAddress>(){
    @Override public HostAddress apply(    InetAddress input){
      return HostAddress.fromString(input.getHostAddress());
    }
  }
));
}
