{
  List<Page> pages=client.execute(prepareGet().setUri(location).build(),new PageResponseHandler());
  int count=0;
  for (  Page page : pages) {
    count+=page.getPositionCount();
  }
  return count;
}
