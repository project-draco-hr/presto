{
  return retry().stopOnIllegalExceptions().runUnchecked(new Callable<RecordCursor>(){
    @Override public RecordCursor call() throws Exception {
      return importClient.getRecords(chunk,columns);
    }
  }
);
}
