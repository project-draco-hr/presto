{
  return ImmutableList.of(new HiveConnectorFactory(name,optionalConfig,getClassLoader(),metastore,context.getTypeManager(),context.getPageIndexerFactory(),context.getNodeManager(),context.getServerInfo()));
}
