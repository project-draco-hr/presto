{
  BlockBuilder builder=createBlockBuilder(TupleInfo.SINGLE_VARBINARY);
  for (  String value : values) {
    if (value == null) {
      builder.appendNull();
    }
 else {
      builder.append(value.getBytes(UTF_8));
    }
  }
  return builder.build();
}
