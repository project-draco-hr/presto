{
  if (usesColumnarTextSerDe(split)) {
    return Optional.<RecordCursor>of(new ColumnarTextHiveRecordCursor<>(columnarTextRecordReader(recordReader),split.getLength(),split.getSchema(),split.getPartitionKeys(),columns));
  }
  return Optional.absent();
}
