{
  PlanFragment fragment=new PlanFragment(new PlanFragmentId("graphviz_plan"),plan,types,plan.getOutputSymbols(),SINGLE_DISTRIBUTION,plan.getId(),new PartitionFunctionBinding(SINGLE_DISTRIBUTION,ImmutableList.of()));
  return GraphvizPrinter.printLogical(ImmutableList.of(fragment));
}
