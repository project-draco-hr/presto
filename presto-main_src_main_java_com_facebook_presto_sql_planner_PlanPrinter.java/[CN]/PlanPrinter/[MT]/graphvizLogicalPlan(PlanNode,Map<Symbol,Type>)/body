{
  PlanFragment fragment=new PlanFragment(new PlanFragmentId("graphviz_plan"),plan,types,singlePartition().getHandle(),ImmutableList.of(plan.getId()),new PartitioningScheme(singlePartition(),plan.getOutputSymbols()));
  return GraphvizPrinter.printLogical(ImmutableList.of(fragment));
}
