{
  return Joiner.on(", ").join(Iterables.transform(slots,new Function<Slot,String>(){
    @Override public String apply(    Slot input){
      return input.getName() + ":" + input.getType();
    }
  }
));
}
