{
  RunLengthEncodedBlock rleBlock=(RunLengthEncodedBlock)block;
  writeRunLengthEncodedBlock(sliceOutput,rleBlock.getRange().getStart(),(int)rleBlock.getRange().length(),rleBlock.getSingleValue());
}
