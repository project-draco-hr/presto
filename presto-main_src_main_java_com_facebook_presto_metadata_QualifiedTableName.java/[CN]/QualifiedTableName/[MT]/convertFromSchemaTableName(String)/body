{
  return new Function<SchemaTableName,QualifiedTableName>(){
    @Override public QualifiedTableName apply(    SchemaTableName input){
      return new QualifiedTableName(catalogName,input.getSchemaName(),input.getTableName());
    }
  }
;
}
