{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Union union=(Union)o;
  if (!relations.equals(union.relations)) {
    return false;
  }
  if (distinct != union.distinct) {
    return false;
  }
  return true;
}
