{
  int pos=(int)getHashPosition(rawHash,mask);
  while (key[pos] != -1) {
    if (positionEqualsCurrentRow(key[pos],position,page.getBlocks())) {
      return key[pos];
    }
    pos=(pos + 1) & mask;
  }
  return -1;
}
