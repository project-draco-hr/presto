{
  int pos=(murmurHash3(hashCursor(cursors) ^ mask)) & mask;
  while (key[pos] != -1) {
    if (positionEqualsCurrentRow(key[pos],cursors)) {
      return key[pos];
    }
    pos=(pos + 1) & mask;
  }
  return -1;
}
