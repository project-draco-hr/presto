{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ShardNodeId that=(ShardNodeId)o;
  return (nodeId == that.nodeId) && Objects.equals(shardUuid,that.shardUuid);
}
