{
  return toStringHelper(this).add("user",getUser()).add("source",source).add("catalog",catalog).add("schema",schema).add("timeZoneKey",timeZoneKey).add("locale",locale).add("remoteUserAddress",remoteUserAddress).add("userAgent",userAgent).add("startTime",startTime).toString();
}
