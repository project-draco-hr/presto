{
  checkNotNull(catalog,"catalog is null");
  return new FullConnectorSession(identity,timeZoneKey,locale,startTime,catalogProperties.getOrDefault(catalog,ImmutableMap.of()),catalog,sessionPropertyManager);
}
