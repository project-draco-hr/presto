{
  this(new TaskStateMachine(checkNotNull(taskId,"taskId is null"),checkNotSameThreadExecutor(executor,"executor is null")),executor,session,checkNotNull(checkNotNull(maxMemory,"maxMemory is null"),"maxMemory is null"),new DataSize(1,MEGABYTE),true,true);
}
