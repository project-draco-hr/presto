{
synchronized (outputItems) {
    return ImmutableMap.copyOf(Maps.transformValues(outputItems.asMap(),new Function<Iterable<?>,Set<?>>(){
      @Override public Set<?> apply(      Iterable<?> values){
        return ImmutableSet.copyOf(values);
      }
    }
));
  }
}
