{
  checkState(connectors.putIfAbsent(catalogName,connectorMetadata) == null,"Catalog '%s' is already registered",catalogName);
  informationSchemas.put(catalogName,new InformationSchemaMetadata(catalogName));
}
