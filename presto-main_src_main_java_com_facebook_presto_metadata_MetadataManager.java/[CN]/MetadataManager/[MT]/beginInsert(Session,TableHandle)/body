{
  ConnectorInsertTableHandle handle=lookupConnectorFor(tableHandle).beginInsert(session.toConnectorSession(),tableHandle.getConnectorHandle());
  return new InsertTableHandle(tableHandle.getConnectorId(),handle);
}
