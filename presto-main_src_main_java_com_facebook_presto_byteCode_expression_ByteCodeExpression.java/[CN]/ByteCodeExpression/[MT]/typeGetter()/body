{
  return new Function<ByteCodeExpression,ParameterizedType>(){
    @Override public ParameterizedType apply(    ByteCodeExpression byteCodeExpression){
      return byteCodeExpression.getType();
    }
  }
;
}
