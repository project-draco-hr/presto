{
  checkNotNull(outputBuffers,"outputBuffers is null");
  if (outputBuffers.isNoMoreBufferIds()) {
    return outputBuffers;
  }
  return new OutputBuffers(outputBuffers.getVersion() + 1,true,outputBuffers.getBufferIds());
}
