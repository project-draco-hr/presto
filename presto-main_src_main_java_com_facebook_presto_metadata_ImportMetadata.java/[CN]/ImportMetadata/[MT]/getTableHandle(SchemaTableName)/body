{
  checkNotNull(tableName,"tableName is null");
  TableHandle tableHandle=client.getTableHandle(tableName);
  if (tableHandle == null) {
    return null;
  }
  return new ImportTableHandle(clientId,new QualifiedTableName(clientId,tableName.getSchemaName(),tableName.getTableName()),tableHandle);
}
