{
  return Iterators.<Block,DictionaryEncodedBlock>transform(sourceBlockStream.iterator(),new Function<Block,DictionaryEncodedBlock>(){
    @Override public DictionaryEncodedBlock apply(    Block input){
      return new DictionaryEncodedBlock(tupleInfo,dictionary,input);
    }
  }
);
}
