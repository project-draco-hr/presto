{
  return Iterators.transform(sourceBlockStream.iterator(),new Function<ValueBlock,DictionaryEncodedBlock>(){
    @Override public DictionaryEncodedBlock apply(    ValueBlock input){
      return new DictionaryEncodedBlock(tupleInfo,dictionary,input);
    }
  }
);
}
