{
  FilterAndProjectOperator filterAndProjectOperator=new FilterAndProjectOperator(operator,FilterFunctions.TRUE_FUNCTION,new Concat(operator.getTupleInfos()));
  return new MaterializedResult(getTuples(filterAndProjectOperator),Iterables.getOnlyElement(filterAndProjectOperator.getTupleInfos()));
}
