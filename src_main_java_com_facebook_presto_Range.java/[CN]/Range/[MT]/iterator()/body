{
  return new AbstractSequentialIterator<Long>(start){
    @Override protected Long computeNext(    Long previous){
      if (previous == end) {
        return null;
      }
      return previous + 1;
    }
  }
;
}
