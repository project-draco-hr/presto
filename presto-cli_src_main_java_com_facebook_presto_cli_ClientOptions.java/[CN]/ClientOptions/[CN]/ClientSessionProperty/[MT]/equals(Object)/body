{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  ClientSessionProperty other=(ClientSessionProperty)obj;
  return Objects.equal(this.catalog,other.catalog) && Objects.equal(this.name,other.name) && Objects.equal(this.value,other.value);
}
