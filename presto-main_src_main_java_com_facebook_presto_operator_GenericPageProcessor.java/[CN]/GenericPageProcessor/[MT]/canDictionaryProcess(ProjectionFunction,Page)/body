{
  Set<Integer> inputChannels=projection.getInputChannels();
  return projection.isDeterministic() && inputChannels.size() == 1 && (inputPage.getBlock(getOnlyElement(inputChannels)) instanceof DictionaryBlock);
}
