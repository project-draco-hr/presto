{
  try {
    OperatorContext operatorContext=driverContext.addOperatorContext(operatorId,NewTableWriterOperator.class.getSimpleName());
    return new NewTableWriterOperator(operatorContext,sourceId,storageManager,nodeIdentifier,columnHandles);
  }
 catch (  IOException e) {
    throw Throwables.propagate(e);
  }
}
