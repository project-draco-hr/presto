{
  DataSource dataSource=splitManager.getSplits(session,node.getTable(),node.getPartitionPredicate(),node.getUpstreamPredicateHint(),materializedViewPartitionPredicate,node.getAssignments());
  return new NodeSplits(node.getId(),dataSource);
}
