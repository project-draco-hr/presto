{
  DataSource dataSource=splitManager.getSplits(session,node.getTable(),node.getPartitionPredicate(),node.getUpstreamPredicateHint(),tableWriterPartitionPredicate,node.getAssignments());
  return new NodeSplits(node.getId(),dataSource);
}
