{
  DataSource dataSource=splitManager.getSplits(session,node.getTable(),context.getInheritedPredicate(),context.getPartitionPredicate(),node.getAssignments());
  return new NodeSplits(node.getId(),dataSource);
}
