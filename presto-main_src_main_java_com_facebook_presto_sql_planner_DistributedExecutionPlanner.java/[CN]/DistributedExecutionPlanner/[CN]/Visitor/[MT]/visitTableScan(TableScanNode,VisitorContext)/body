{
  DataSource dataSource=splitManager.getSplits(node.getId(),session,node.getTable(),context.getInheritedPredicate(),context.getPartitionPredicate(),node.getAssignments());
  return new NodeSplits(node.getId(),dataSource);
}
