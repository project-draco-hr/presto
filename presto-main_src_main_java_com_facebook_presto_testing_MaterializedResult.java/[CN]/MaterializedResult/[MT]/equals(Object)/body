{
  if (obj == this) {
    return true;
  }
  if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  MaterializedResult o=(MaterializedResult)obj;
  return Objects.equal(types,o.types) && Objects.equal(rows,o.rows) && Objects.equal(setSessionProperties,o.setSessionProperties)&& Objects.equal(resetSessionProperties,o.resetSessionProperties);
}
