{
  return new Function<Expression,Expression>(){
    @Override public Expression apply(    Expression input){
      if (input instanceof AliasedExpression) {
        return ((AliasedExpression)input).getExpression();
      }
      return input;
    }
  }
;
}
