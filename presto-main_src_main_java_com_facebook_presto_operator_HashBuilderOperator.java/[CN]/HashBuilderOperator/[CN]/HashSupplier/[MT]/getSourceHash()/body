{
  return Futures.transform(hashFuture,new Function<HashData,SourceHash>(){
    @Override public SourceHash apply(    HashData hashData){
      return new SourceHash(new ChannelHash(hashData.channelHash),hashData.pagesIndex);
    }
  }
);
}
