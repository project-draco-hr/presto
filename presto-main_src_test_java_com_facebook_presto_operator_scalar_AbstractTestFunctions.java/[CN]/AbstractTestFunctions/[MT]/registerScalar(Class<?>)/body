{
  Metadata metadata=functionAssertions.getMetadata();
  List<ParametricFunction> functions=new FunctionListBuilder(metadata.getTypeManager()).scalar(clazz).getFunctions();
  metadata.getFunctionRegistry().addFunctions(functions);
}
