{
  return retry().stopOnIllegalExceptions().run("getPartitions",new Callable<List<CassandraPartition>>(){
    @Override public List<CassandraPartition> call(){
      return session.getPartitions(key.getTable(),key.getFilterPrefix());
    }
  }
);
}
