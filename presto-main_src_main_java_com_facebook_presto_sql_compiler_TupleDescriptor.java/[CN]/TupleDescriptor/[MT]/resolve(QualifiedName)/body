{
  return ImmutableList.copyOf(Iterables.filter(slots,new Predicate<NamedSlot>(){
    @Override public boolean apply(    NamedSlot input){
      return input.getName().isPresent() && input.getName().get().hasSuffix(name);
    }
  }
));
}
