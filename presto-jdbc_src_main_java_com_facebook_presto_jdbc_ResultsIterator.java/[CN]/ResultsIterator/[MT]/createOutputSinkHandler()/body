{
  return new OutputSink.OutputSinkHandler(){
    @Override public void process(    Tuple... tuples){
      List<Object> row=new ArrayList<>();
      for (      Tuple tuple : tuples) {
        row.addAll(tuple.toValues());
      }
      rowBuffer.add(unmodifiableList(row));
    }
  }
;
}
