{
  if ((state.getKey() == null) || state.getKey().isNull(0) || compare(keyType.compareTo(key,position,state.getKey(),0),min)) {
    state.setKey(key.getSingleValueBlock(position));
    state.setValue(value.getSingleValueBlock(position));
  }
}
