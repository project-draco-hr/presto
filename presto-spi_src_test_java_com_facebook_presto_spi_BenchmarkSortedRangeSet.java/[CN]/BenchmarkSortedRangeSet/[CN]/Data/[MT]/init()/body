{
  ranges=new ArrayList<>();
  int factor=0;
  for (int i=0; i < 10000; i++) {
    int from=ThreadLocalRandom.current().nextInt(100) + factor * 100;
    int to=ThreadLocalRandom.current().nextInt(100) + (factor + 1) * 100;
    factor++;
    ranges.add(new Range(new Marker(new SerializableNativeValue(Integer.class,from),Marker.Bound.ABOVE),new Marker(new SerializableNativeValue(Integer.class,to),Marker.Bound.BELOW)));
  }
}
