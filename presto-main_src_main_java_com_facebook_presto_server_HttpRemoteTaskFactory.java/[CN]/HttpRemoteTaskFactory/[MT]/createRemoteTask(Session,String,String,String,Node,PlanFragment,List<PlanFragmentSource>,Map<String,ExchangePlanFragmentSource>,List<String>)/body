{
  return new HttpRemoteTask(session,queryId,stageId,taskId,locationFactory.createTaskLocation(node,taskId),fragment,splits,exchangeSources,outputIds,httpClient,taskInfoCodec,queryFragmentRequestCodec);
}
