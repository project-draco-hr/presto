{
  this.operatorId=operatorId;
  this.sampleRatio=sampleRatio;
  this.tupleInfos=ImmutableList.<TupleInfo>builder().addAll(checkNotNull(sourceTupleInfos,"sourceTupleInfos is null")).add(TupleInfo.SINGLE_LONG).build();
}
