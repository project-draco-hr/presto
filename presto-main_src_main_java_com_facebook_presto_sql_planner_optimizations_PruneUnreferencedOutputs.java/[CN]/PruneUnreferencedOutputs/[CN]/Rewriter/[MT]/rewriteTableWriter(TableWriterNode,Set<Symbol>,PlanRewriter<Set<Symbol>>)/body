{
  Set<Symbol> expectedInputs=ImmutableSet.copyOf(node.getInputSymbols());
  PlanNode source=planRewriter.rewrite(node.getSource(),expectedInputs);
  return new TableWriterNode(node.getId(),source,node.getTableHandle(),node.getInputSymbols(),node.getInputTypes(),node.getColumnHandles(),node.getOutputTypes());
}
