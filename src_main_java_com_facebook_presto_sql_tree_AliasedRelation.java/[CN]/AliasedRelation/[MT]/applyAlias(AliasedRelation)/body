{
  return new Function<QualifiedName,QualifiedName>(){
    @Override public QualifiedName apply(    QualifiedName input){
      return QualifiedName.of(node.getAlias(),input.getSuffix());
    }
  }
;
}
