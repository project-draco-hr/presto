{
  assertRewrite("SELECT * FROM (\n" + "   SELECT orderkey id, totalprice value FROM ORDERS\n" + ") U","SELECT U.id id, U.value value FROM (\n" + "   SELECT orderkey id, totalprice value FROM ORDERS\n" + ") U");
}
