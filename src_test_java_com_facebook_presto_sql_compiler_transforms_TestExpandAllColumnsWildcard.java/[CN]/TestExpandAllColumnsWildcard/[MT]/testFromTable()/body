{
  assertRewrite("SELECT * FROM ORDERS","SELECT ORDERS.orderkey orderkey, " + "ORDERS.custkey custkey, " + "ORDERS.totalprice totalprice, "+ "ORDERS.orderdate orderdate, "+ "ORDERS.orderstatus orderstatus, "+ "ORDERS.orderpriority orderpriority, "+ "ORDERS.clerk clerk, "+ "ORDERS.shippriority shippriority, "+ "ORDERS.comment comment\n"+ "FROM ORDERS");
}
