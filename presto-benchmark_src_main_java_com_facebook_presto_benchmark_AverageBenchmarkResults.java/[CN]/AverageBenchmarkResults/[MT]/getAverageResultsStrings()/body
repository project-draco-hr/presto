{
  return Maps.transformValues(resultsSum,new Function<Long,String>(){
    @Override public String apply(    @Nullable Long input){
      return String.format("%,3.2f",1.0 * input / resultsCount);
    }
  }
);
}
