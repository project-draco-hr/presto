{
  Session session=Session.builder().setUser("user").setSource("test").setCatalog("tpch").setSchema("sf1000").setTimeZoneKey(UTC_KEY).setLocale(ENGLISH).build();
  try (QueryRunner queryRunner=createQueryRunner(session)){
    queryRunner.execute(session,"SELECT COUNT(*), clerk FROM orders GROUP BY clerk");
  }
 }
