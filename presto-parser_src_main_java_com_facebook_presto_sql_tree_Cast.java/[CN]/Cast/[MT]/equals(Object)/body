{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Cast cast=(Cast)o;
  if (!expression.equals(cast.expression)) {
    return false;
  }
  if (!type.equals(cast.type)) {
    return false;
  }
  return true;
}
