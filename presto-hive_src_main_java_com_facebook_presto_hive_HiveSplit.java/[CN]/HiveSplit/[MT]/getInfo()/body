{
  return ImmutableMap.builder().put("path",path.toString()).put("start",start).put("length",length).put("hosts",addresses).put("database",database).put("table",table).put("partitionId",partitionId).build();
}
