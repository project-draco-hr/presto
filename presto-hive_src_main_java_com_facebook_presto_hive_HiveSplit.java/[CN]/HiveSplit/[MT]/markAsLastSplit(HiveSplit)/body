{
  if (split.isLastSplit()) {
    return split;
  }
  return new HiveSplit(split.getClientId(),split.getDatabase(),split.getTable(),split.getPartitionId(),true,split.getPath(),split.getStart(),split.getLength(),split.getSchema(),split.getPartitionKeys(),split.getAddresses());
}
