{
  Slice slice=buffer.slice();
  sliceOutput.appendInt(slice.length()).appendInt(tupleCount).appendLong(blockStartPosition).writeBytes(slice);
  buffer.reset();
  blockStartPosition+=tupleCount;
  tupleCount=0;
}
