{
  return Lists.transform(positions,new Function<Integer,T>(){
    @Override public T apply(    Integer position){
      checkPositionIndex(position,values.size());
      return values.get(position);
    }
  }
);
}
