{
synchronized (this) {
    if (subStageOutputBuffers.isNoMoreBufferIds()) {
      return;
    }
    subStageOutputBuffers=subStageOutputBuffers.withNoMoreBufferIds();
  }
  updateSubStageOutputBuffers();
}
