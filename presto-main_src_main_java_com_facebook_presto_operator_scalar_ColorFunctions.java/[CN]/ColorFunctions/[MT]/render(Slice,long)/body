{
  StringBuilder builder=new StringBuilder(value.length());
  builder.append(ansiColorEscape(color)).append(value.toString(UTF_8)).append(ANSI_RESET);
  return Slices.copiedBuffer(builder.toString(),UTF_8);
}
