{
  return transform(doubleSequence(start,step,items),new Function<Double,Float>(){
    @Nullable @Override public Float apply(    @Nullable Double input){
      if (input == null) {
        return null;
      }
      return input.floatValue();
    }
  }
);
}
