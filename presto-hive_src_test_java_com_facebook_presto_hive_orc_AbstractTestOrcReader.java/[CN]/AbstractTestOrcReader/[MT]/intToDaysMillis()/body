{
  return new Function<Integer,Long>(){
    @Nullable @Override public Long apply(    @Nullable Integer input){
      if (input == null) {
        return null;
      }
      return TimeUnit.DAYS.toMillis(input);
    }
  }
;
}
