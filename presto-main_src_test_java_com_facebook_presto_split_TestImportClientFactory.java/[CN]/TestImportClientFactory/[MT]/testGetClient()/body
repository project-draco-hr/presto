{
  ImportClientManager factory=new ImportClientManager(ImmutableMap.<String,ImportClientFactory>of("apple",new MockImportClientFactor("apple"),"banana",new MockImportClientFactor("banana")));
  assertInstanceOf(factory.getClient("apple"),ImportClient.class);
  assertInstanceOf(factory.getClient("banana"),ImportClient.class);
}
