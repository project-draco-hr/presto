{
  if (this == o) {
    return true;
  }
  if ((o == null) || (getClass() != o.getClass())) {
    return false;
  }
  RaptorPartitioningHandle that=(RaptorPartitioningHandle)o;
  return (distributionId == that.distributionId) && Objects.equals(bucketToNode,that.bucketToNode);
}
