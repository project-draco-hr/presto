{
  TaskInfo outputTask=simpleTaskManager.getTaskInfo(outputTaskId);
  return new QueryInfo(queryId,outputTask.getTupleInfos(),ImmutableList.of("out"),outputTask.getState(),"out",ImmutableMap.<String,List<TaskInfo>>of("out",ImmutableList.of(outputTask)));
}
