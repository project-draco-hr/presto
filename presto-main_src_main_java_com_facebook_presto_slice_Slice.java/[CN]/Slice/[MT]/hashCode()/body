{
  if (hash != 0) {
    return hash;
  }
  int result=1;
  for (int i=0; i < length(); i++) {
    result=(31 * result) + unsafe.getByte(base,this.address + i);
  }
  if (result == 0) {
    result=1;
  }
  hash=result;
  return hash;
}
