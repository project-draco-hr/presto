{
  checkNotNull(nativeProvider,"nativeProvider is null");
  checkNotNull(importProvider,"importProvider is null");
  dataStreamProviderMap=ImmutableMap.<DataSourceType,DataStreamProvider>builder().put(DataSourceType.NATIVE,nativeProvider).put(DataSourceType.IMPORT,importProvider).build();
}
