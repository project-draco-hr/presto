{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  final SharedBufferInfo other=(SharedBufferInfo)obj;
  return Objects.equal(this.state,other.state) && Objects.equal(this.pagesAdded,other.pagesAdded) && Objects.equal(this.buffers,other.buffers)&& Objects.equal(this.masterSequenceId,other.masterSequenceId);
}
