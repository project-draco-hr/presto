{
  Statement parsed=SqlParser.createStatement(query);
  if (!parsed.equals(expected)) {
    fail(format("expected\n\n%s\n\nto parse as\n\n%s\n\nbut was\n\n%s\n",indent(query),indent(SqlFormatter.toString(expected)),indent(SqlFormatter.toString(parsed))));
  }
}
