{
  this.taskOutput=taskOutput;
  LocalExecutionPlanner planner=new LocalExecutionPlanner(metadata,sourceProvider,fragment.getSymbols(),split,null,exchangeSources,operatorStats,sourceHashProviderFactory,maxOperatorMemoryUsage);
  operator=planner.plan(fragment.getRoot());
}
