{
  SqlTaskExecution task=new SqlTaskExecution(session,queryId,stageId,taskId,location,fragment,initialOutputIds,pageBufferMax,dataStreamProvider,exchangeOperatorFactory,metadata,shardExecutor,maxOperatorMemoryUsage);
  task.start(taskMasterExecutor,initialSources);
  return task;
}
