{
  SqlTaskExecution task=new SqlTaskExecution(session,taskId,location,fragment,planner,maxBufferSize,shardExecutor,maxTaskMemoryUsage,minFlushSize,queryMonitor,taskMasterExecutor,globalStats);
  try (SetThreadName setThreadName=new SetThreadName("Task-%s",taskId)){
    task.start(taskMasterExecutor);
    return task;
  }
 }
