{
  SqlTaskExecution task=new SqlTaskExecution(session,queryId,stageId,taskId,location,fragment,pageBufferMax,dataStreamProvider,exchangeOperatorFactory,metadata,shardExecutor,maxOperatorMemoryUsage);
  task.start(taskMasterExecutor);
  return task;
}
