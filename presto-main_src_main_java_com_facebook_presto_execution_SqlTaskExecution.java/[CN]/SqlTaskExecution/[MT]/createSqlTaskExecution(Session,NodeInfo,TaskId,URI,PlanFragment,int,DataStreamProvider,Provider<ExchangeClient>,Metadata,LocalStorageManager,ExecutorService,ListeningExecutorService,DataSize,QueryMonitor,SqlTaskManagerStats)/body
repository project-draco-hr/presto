{
  SqlTaskExecution task=new SqlTaskExecution(session,nodeInfo,taskId,location,fragment,pageBufferMax,dataStreamProvider,exchangeClientProvider,metadata,storageManager,shardExecutor,maxOperatorMemoryUsage,queryMonitor,taskMasterExecutor,globalStats);
  task.start(taskMasterExecutor);
  return task;
}
