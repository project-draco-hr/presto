{
  checkNotHoldsLock(this);
synchronized (this) {
    if (finishing) {
      return;
    }
    finishing=true;
  }
  readWaiter.finishAndNotifyWaiters();
  if (isFinished()) {
    onFinish.accept(this);
  }
}
