{
  Map<String,String> properties=ImmutableMap.<String,String>builder().put("metadata.db.type","h2").build();
  DatabaseConfig expected=new DatabaseConfig().setDatabaseType("h2");
  assertFullMapping(properties,expected);
}
