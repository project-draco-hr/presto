{
  try (SetThreadName ignored=new SetThreadName("UpdateResponseHandler-%s",taskId)){
    try {
      needsUpdate.set(true);
      requestFailed(cause);
    }
  finally {
      scheduleUpdate();
    }
  }
 }
