{
  try (SetThreadName setThreadName=new SetThreadName("ContinuousTaskInfoFetcher-%s",taskId)){
synchronized (this) {
      future=null;
    }
    try {
      requestFailed(cause);
    }
  finally {
      scheduleNextRequest();
    }
  }
 }
