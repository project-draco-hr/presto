{
  GroupByOperator groupBy=new GroupByOperator(newGroupColumn());
  PipelinedAggregationOperator aggregation=new PipelinedAggregationOperator(groupBy,newAggregateColumn(),LongSumAggregation.PROVIDER);
  assertTupleStreamEquals(aggregation,tupleStreamBuilder(VARIABLE_BINARY,FIXED_INT_64).append("apple").append(10L).append("banana").append(17L).append("cherry").append(15L).append("date").append(6L).build());
}
