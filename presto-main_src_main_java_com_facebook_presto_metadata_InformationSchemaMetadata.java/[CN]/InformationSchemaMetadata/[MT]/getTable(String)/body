{
  return new Function<String,QualifiedTableName>(){
    @Override public QualifiedTableName apply(    String table){
      return new QualifiedTableName(catalogName,INFORMATION_SCHEMA,table);
    }
  }
;
}
