{
  return ImmutableList.copyOf(filter(transform(tasks.values(),new Function<TaskOutput,TaskInfo>(){
    @Override public TaskInfo apply(    TaskOutput taskOutput){
      try {
        return taskOutput.getTaskInfo();
      }
 catch (      Exception ignored) {
        return null;
      }
    }
  }
),Predicates.notNull()));
}
