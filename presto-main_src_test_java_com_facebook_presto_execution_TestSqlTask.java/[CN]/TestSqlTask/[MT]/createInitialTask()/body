{
  TaskId taskId=new TaskId("query","stage","task" + nextTaskId.incrementAndGet());
  URI location=URI.create("fake://task/" + taskId);
  return new SqlTask(taskId,"test",location,new QueryContext(false,new DataSize(1,MEGABYTE),new MemoryPool(new MemoryPoolId("test"),new DataSize(1,GIGABYTE),false),new MemoryPool(new MemoryPoolId("testSystem"),new DataSize(1,GIGABYTE),false),taskNotificationExecutor),sqlTaskExecutionFactory,taskNotificationExecutor,Functions.<SqlTask>identity(),new DataSize(32,MEGABYTE));
}
