{
  TaskId taskId=new TaskId("query","stage","task" + nextTaskId.incrementAndGet());
  URI location=URI.create("fake://task/" + taskId);
  return new SqlTask(taskId,"test",location,new QueryContext(false,new DataSize(1,MEGABYTE),taskNotificationExecutor),sqlTaskExecutionFactory,taskNotificationExecutor,Functions.<SqlTask>identity(),new DataSize(32,MEGABYTE));
}
