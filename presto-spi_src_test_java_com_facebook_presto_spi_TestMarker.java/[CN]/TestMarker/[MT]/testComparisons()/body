{
  ImmutableList<Marker> markers=ImmutableList.of(Marker.lowerUnbounded(Long.class),Marker.above(0L),Marker.below(1L),Marker.exactly(1L),Marker.above(1L),Marker.below(2L),Marker.upperUnbounded(Long.class));
  Assert.assertTrue(Ordering.natural().isStrictlyOrdered(markers));
  for (int i=0; i < markers.size(); i++) {
    for (int j=0; j < markers.size(); j++) {
      Assert.assertTrue(markers.get(i).compareTo(markers.get(j)) == Integer.compare(i,j));
    }
  }
}
