{
  if (totalCount == 1) {
    output.appendDouble(0.0);
    return;
  }
  if (newPeerGroup) {
    rank+=count;
    count=1;
  }
 else {
    count++;
  }
  output.appendDouble(((double)(rank - 1)) / (totalCount - 1));
}
