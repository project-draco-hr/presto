{
  if (totalCount == 1) {
    output.append(0.0);
    return;
  }
  if (newPeerGroup) {
    rank+=count;
    count=1;
  }
 else {
    count++;
  }
  output.append(((double)(rank - 1)) / (totalCount - 1));
}
