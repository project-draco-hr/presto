{
  if (count == 0) {
    out.appendNull();
    return;
  }
  out.append(formatApproximateResult(sum,sumError(calculator.getCount(),count,calculator.getM2(),calculator.getMean()),confidence,false));
}
