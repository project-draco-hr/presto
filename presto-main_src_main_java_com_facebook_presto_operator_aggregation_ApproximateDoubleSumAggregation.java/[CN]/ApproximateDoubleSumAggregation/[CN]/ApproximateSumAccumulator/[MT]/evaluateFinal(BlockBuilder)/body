{
  if (count == 0) {
    out.appendNull();
    return;
  }
  String result=formatApproximateResult(sum,sumError(calculator.getCount(),count,calculator.getM2(),calculator.getMean()),confidence,false);
  out.appendSlice(Slices.utf8Slice(result));
}
