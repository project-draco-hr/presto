{
  CliBuilder<Runnable> builder=Cli.<Runnable>builder("presto").withDefaultCommand(Console.class).withCommand(Console.class).withCommand(Help.class);
  Cli<Runnable> cli=builder.build();
  cli.parse(args).run();
}
