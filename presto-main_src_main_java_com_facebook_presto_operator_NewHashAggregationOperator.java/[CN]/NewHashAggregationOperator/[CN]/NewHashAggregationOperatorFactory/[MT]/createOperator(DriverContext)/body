{
  checkState(!closed,"Factory is already closed");
  OperatorContext operatorContext=driverContext.addOperatorContext(operatorId,NewHashAggregationOperator.class.getSimpleName());
  return new NewHashAggregationOperator(operatorContext,groupByTupleInfo,groupByChannel,step,functionDefinitions,expectedGroups);
}
