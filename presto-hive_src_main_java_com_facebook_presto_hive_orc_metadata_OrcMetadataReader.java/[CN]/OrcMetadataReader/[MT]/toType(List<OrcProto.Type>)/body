{
  return ImmutableList.copyOf(Iterables.transform(types,new Function<OrcProto.Type,OrcType>(){
    @Override public OrcType apply(    OrcProto.Type type){
      return toType(type);
    }
  }
));
}
