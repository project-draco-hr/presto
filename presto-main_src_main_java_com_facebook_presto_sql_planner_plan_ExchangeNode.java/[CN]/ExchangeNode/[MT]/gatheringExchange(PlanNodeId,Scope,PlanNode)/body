{
  return new ExchangeNode(id,ExchangeNode.Type.GATHER,scope,new PartitioningScheme(singlePartition(),child.getOutputSymbols()),ImmutableList.of(child),ImmutableList.of(child.getOutputSymbols()));
}
