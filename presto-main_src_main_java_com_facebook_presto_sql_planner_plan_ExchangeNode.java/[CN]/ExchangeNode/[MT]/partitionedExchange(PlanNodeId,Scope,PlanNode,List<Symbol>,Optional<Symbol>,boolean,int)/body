{
  return partitionedExchange(id,scope,child,new PartitioningScheme(fixedHashPartitioning(partitionCount,partitioningColumns),child.getOutputSymbols(),hashColumns,nullsReplicated,Optional.empty()));
}
