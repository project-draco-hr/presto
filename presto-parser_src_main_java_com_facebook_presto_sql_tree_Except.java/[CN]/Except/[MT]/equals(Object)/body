{
  if (this == obj) {
    return true;
  }
  if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  Except o=(Except)obj;
  return Objects.equal(left,o.left) && Objects.equal(right,o.right) && Objects.equal(distinct,o.distinct);
}
