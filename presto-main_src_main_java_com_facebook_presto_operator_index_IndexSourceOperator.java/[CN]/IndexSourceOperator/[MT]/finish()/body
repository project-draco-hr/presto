{
  Operator delegate;
synchronized (this) {
    delegate=getSource();
    if (delegate == null) {
      source=new FinishedOperator(operatorContext,types);
      return;
    }
  }
  delegate.finish();
}
