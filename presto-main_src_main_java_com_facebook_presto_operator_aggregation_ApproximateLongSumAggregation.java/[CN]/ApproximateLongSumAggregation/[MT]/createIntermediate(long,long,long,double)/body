{
  Slice slice=Slices.allocate(3 * SIZE_OF_LONG + SIZE_OF_DOUBLE);
  slice.setLong(COUNT_OFFSET,count);
  slice.setLong(SAMPLES_OFFSET,samples);
  slice.setLong(SUM_OFFSET,sum);
  slice.setDouble(VARIANCE_OFFSET,variance);
  return slice;
}
