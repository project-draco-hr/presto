{
  long count=counts.get(groupId);
  if (count == 0) {
    output.appendNull();
    return;
  }
  output.append(formatApproximateResult(sums.get(groupId),sumError(samples.get(groupId),count,sums.get(groupId),variances.get(groupId)),confidence,true));
}
