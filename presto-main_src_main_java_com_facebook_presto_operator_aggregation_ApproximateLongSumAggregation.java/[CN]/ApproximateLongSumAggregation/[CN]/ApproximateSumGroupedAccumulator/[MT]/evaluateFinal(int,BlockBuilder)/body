{
  long count=counts.get(groupId);
  if (count == 0) {
    output.appendNull();
    return;
  }
  output.append(formatApproximateResult(sums.get(groupId),sumError(samples.get(groupId),count,m2s.get(groupId),means.get(groupId)),confidence,true));
}
