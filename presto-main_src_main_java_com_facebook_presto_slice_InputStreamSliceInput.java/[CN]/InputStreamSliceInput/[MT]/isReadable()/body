{
  try {
    int value=pushbackInputStream.read();
    if (value == -1) {
      return false;
    }
    pushbackInputStream.unread(value);
    return true;
  }
 catch (  IOException e) {
    throw Throwables.propagate(e);
  }
}
