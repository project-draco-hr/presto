{
  if (blockCursor == null) {
    return false;
  }
  hasAdvanced=true;
  if (!blockCursor.advanceNextValue()) {
    if (iterator.hasNext()) {
      blockCursor=iterator.next().cursor();
      blockCursor.advanceNextPosition();
    }
 else {
      blockCursor=null;
      return false;
    }
  }
  return true;
}
