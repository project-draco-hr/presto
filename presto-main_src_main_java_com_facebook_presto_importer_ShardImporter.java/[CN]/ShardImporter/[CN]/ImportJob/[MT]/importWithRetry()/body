{
  for (int i=0; i < importAttempts; i++) {
    try {
      importShard();
      return;
    }
 catch (    IOException e) {
      log.warn(e,"error importing shard");
    }
  }
  log.error("shard import failed");
}
