{
  return IterableTransformer.<Symbol>on(getOutputSymbols()).toMap(outputToSourceSymbolFunction(sourceIndex)).<QualifiedNameReference>transformValues(symbolToQualifiedNameReference()).immutableMap();
}
