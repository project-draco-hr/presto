{
  return IterableTransformer.<Symbol>on(getOutputSymbols()).toMap(outputToSourceSymbolFunction(sourceIndex)).inverse().<QualifiedNameReference>transformValues(symbolToQualifiedNameReference()).immutableMap();
}
