{
  if (cursor.isNull(0)) {
    return currentMin;
  }
  Slice value=cursor.getSlice(0);
  if (currentMin == null) {
    return value;
  }
 else {
    return Ordering.natural().min(currentMin,value);
  }
}
