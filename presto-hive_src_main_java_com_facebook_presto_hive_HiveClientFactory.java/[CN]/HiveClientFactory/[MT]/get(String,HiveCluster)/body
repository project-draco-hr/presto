{
  CachingHiveMetastore metastore=metastores.getUnchecked(hiveCluster);
  return new HiveClient(clientId,maxSplitSize.toBytes(),maxOutstandingSplits,masSplitIteratorThreads,partitionBatchSize,metastore,hdfsEnvironment,executorService);
}
