{
  return new QueryContext(false,new DataSize(10,MEGABYTE),executor).addTaskContext(new TaskStateMachine(new TaskId("query","stage","task"),checkNotSameThreadExecutor(executor,"executor is null")),session,checkNotNull(maxMemory,"maxMemory is null"),new DataSize(1,MEGABYTE),true,true);
}
