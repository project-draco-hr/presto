{
  return TreeRewriter.rewriteWith(new NodeRewriter<Void>(){
    @Override public Node rewriteSlotReference(    SlotReference node,    Void context,    TreeRewriter<Void> treeRewriter){
      return new SlotReference(canonicalize(node.getSlot()));
    }
  }
,value);
}
