{
  if (this == obj) {
    return true;
  }
  if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  Window o=(Window)obj;
  return Objects.equal(partitionBy,o.partitionBy) && Objects.equal(orderBy,o.orderBy) && Objects.equal(frame,o.frame);
}
