{
  long millis=TimeUnit.NANOSECONDS.toMillis(threadUsageNanos);
  int priorityLevel;
  if (millis < 50) {
    priorityLevel=0;
  }
 else   if (millis < 100) {
    priorityLevel=1;
  }
 else   if (millis < 200) {
    priorityLevel=2;
  }
 else   if (millis < 400) {
    priorityLevel=3;
  }
 else {
    priorityLevel=4;
  }
  return priorityLevel;
}
