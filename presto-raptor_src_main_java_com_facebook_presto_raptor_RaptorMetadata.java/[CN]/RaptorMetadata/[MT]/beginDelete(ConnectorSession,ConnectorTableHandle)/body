{
  RaptorTableHandle handle=checkType(tableHandle,RaptorTableHandle.class,"tableHandle");
  long transactionId=shardManager.beginTransaction();
  return new RaptorTableHandle(connectorId,handle.getSchemaName(),handle.getTableName(),handle.getTableId(),OptionalLong.of(transactionId),handle.getSampleWeightColumnHandle());
}
