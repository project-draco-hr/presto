{
  return Objects.toStringHelper(this).add("positionCount",positionCount).add("tupleInfo",tupleInfo).add("rawPositionOffset",rawPositionOffset).add("rawPositionCount",rawPositionCount).add("rawOffset",rawOffset).add("slice",slice).toString();
}
