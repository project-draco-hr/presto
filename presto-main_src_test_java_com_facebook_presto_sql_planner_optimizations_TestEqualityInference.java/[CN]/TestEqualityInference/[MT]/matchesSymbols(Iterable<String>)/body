{
  final Set<Symbol> symbolSet=IterableTransformer.<String>on(symbols).transform(new Function<String,Symbol>(){
    @Override public Symbol apply(    String symbol){
      return new Symbol(symbol);
    }
  }
).set();
  return new Predicate<Symbol>(){
    @Override public boolean apply(    Symbol symbol){
      return symbolSet.contains(symbol);
    }
  }
;
}
