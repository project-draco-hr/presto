{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  PartitionFunctionBinding that=(PartitionFunctionBinding)o;
  return Objects.equals(partitioningHandle,that.partitioningHandle) && Objects.equals(outputLayout,that.outputLayout) && Objects.equals(partitioningColumns,that.partitioningColumns)&& replicateNulls == that.replicateNulls && Objects.equals(bucketToPartition,that.bucketToPartition);
}
