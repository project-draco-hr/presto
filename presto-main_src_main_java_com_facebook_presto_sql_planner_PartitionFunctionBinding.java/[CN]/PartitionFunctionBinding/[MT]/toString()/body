{
  return toStringHelper(this).add("partitioningHandle",partitioningHandle).add("outputLayout",outputLayout).add("partitioningChannels",partitionFunctionArguments).add("hashChannel",hashColumn).add("replicateNulls",replicateNulls).add("bucketToPartition",bucketToPartition).toString();
}
