{
  return toStringHelper(this).add("partitioningHandle",partitioningHandle).add("partitioningChannels",partitioningColumns).add("hashChannel",hashColumn).add("replicateNulls",replicateNulls).add("bucketToPartition",bucketToPartition).toString();
}
