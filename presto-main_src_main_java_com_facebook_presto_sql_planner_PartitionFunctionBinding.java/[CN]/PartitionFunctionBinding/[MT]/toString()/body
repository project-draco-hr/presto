{
  return toStringHelper(this).add("functionHandle",functionHandle).add("partitioningChannels",partitioningColumns).add("hashChannel",hashColumn).add("replicateNulls",replicateNulls).add("partitionCount",partitionCount).toString();
}
