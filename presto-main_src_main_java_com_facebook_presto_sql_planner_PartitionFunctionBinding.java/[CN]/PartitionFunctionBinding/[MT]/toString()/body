{
  return toStringHelper(this).add("functionId",functionHandle).add("partitioningChannels",partitioningChannels).add("hashChannel",hashChannel).add("replicateNulls",replicateNulls).add("partitionCount",partitionCount).toString();
}
