{
  if (this == o) {
    return true;
  }
  if ((o == null) || (getClass() != o.getClass())) {
    return false;
  }
  BucketNode that=(BucketNode)o;
  return (bucketNumber == that.bucketNumber) && Objects.equals(nodeIdentifier,that.nodeIdentifier);
}
