{
  HiveOutputTableHandle handle=checkType(tableHandle,HiveOutputTableHandle.class,"tableHandle");
  return new HivePageSink(handle.getSchemaName(),handle.getTableName(),handle.getInputColumns(),handle.getHiveStorageFormat(),new Path(handle.getWritePath()),handle.getFilePrefix(),typeManager,hdfsEnvironment);
}
