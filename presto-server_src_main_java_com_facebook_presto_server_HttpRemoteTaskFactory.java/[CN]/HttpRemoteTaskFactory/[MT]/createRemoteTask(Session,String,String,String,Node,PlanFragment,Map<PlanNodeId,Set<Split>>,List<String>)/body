{
  return new HttpRemoteTask(session,queryId,stageId,taskId,locationFactory.createTaskLocation(node,taskId),fragment,fixedSources,outputIds,httpClient,taskInfoCodec,queryFragmentRequestCodec,splitCodec);
}
