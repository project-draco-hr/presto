{
  return new HttpRemoteTask(session,queryId,stageId,taskId,locationFactory.createTaskLocation(node,taskId),fragment,fixedSources,initialOutputIds,httpClient,taskInfoCodec,queryFragmentRequestCodec,splitCodec);
}
