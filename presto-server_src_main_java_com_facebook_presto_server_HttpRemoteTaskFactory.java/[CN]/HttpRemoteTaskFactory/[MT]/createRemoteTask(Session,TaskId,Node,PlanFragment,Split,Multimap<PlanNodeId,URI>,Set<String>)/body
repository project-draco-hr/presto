{
  return new HttpRemoteTask(session,taskId,node,locationFactory.createTaskLocation(node,taskId),fragment,initialSplit,initialExchangeLocations,initialOutputIds,httpClient,maxConsecutiveErrorCount,minErrorDuration,taskInfoCodec,taskUpdateRequestCodec);
}
