{
  return new HttpRemoteTask(session,queryId,stageId,taskId,node,locationFactory.createTaskLocation(node,taskId),fragment,initialExchangeLocations,initialOutputIds,httpClient,taskInfoCodec,queryFragmentRequestCodec,splitCodec);
}
