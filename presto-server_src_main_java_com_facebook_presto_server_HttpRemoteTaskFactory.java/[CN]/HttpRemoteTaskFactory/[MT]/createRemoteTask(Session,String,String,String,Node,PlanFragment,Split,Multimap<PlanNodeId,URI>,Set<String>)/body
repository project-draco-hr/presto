{
  return new HttpRemoteTask(session,queryId,stageId,taskId,node,locationFactory.createTaskLocation(node,taskId),fragment,initialSplit,initialExchangeLocations,initialOutputIds,httpClient,taskInfoCodec,taskUpdateRequestCodec);
}
