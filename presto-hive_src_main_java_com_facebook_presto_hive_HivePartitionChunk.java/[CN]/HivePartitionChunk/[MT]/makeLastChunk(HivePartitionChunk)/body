{
  if (chunk.isLastChunk()) {
    return chunk;
  }
 else {
    return new HivePartitionChunk(chunk.getPartitionName(),true,chunk.getPath(),chunk.getStart(),chunk.getLength(),chunk.getSchema(),chunk.getPartitionKeys(),chunk.getColumns(),chunk.getHosts());
  }
}
