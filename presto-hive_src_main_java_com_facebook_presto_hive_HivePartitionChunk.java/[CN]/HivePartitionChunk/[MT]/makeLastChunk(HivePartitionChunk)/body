{
  if (chunk.isLastChunk()) {
    return chunk;
  }
 else {
    return new HivePartitionChunk(chunk.getClientId(),chunk.getPartitionName(),true,chunk.getPath(),chunk.getStart(),chunk.getLength(),chunk.getSchema(),chunk.getPartitionKeys(),chunk.getHosts());
  }
}
