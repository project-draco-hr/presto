{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  SimpleDomain that=(SimpleDomain)o;
  return Objects.equals(this.nullAllowed,that.nullAllowed) && Objects.equals(this.ranges,that.ranges);
}
