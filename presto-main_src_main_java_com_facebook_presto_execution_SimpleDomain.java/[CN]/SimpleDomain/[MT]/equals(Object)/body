{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  SimpleDomain that=(SimpleDomain)o;
  return Objects.equal(this.nullAllowed,that.nullAllowed) && Objects.equal(this.ranges,that.ranges);
}
