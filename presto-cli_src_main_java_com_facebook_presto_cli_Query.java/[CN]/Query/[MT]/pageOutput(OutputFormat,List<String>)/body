{
  ignoreUserInterrupt.set(true);
  try (Writer writer=createWriter(Pager.create());OutputHandler handler=createOutputHandler(format,writer,fieldNames)){
    handler.processRows(client);
  }
 catch (  IOException e) {
    throw Throwables.propagate(e);
  }
}
