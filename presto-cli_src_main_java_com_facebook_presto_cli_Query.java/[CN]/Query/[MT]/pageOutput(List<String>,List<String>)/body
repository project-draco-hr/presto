{
  ignoreUserInterrupt.set(true);
  try (Pager pager=Pager.create(pagerCommand);OutputHandler handler=new AlignedTuplePrinter(fieldNames,createWriter(pager))){
    processOutput(client,handler);
  }
 catch (  IOException e) {
    throw Throwables.propagate(e);
  }
}
