{
switch (outputFormat) {
case CSV:
case CSV_HEADER:
    return new CSVPrinter(writer,',');
case TSV:
case TSV_HEADER:
  return new CSVPrinter(writer,'\t');
}
throw new RuntimeException(outputFormat + " not supported");
}
