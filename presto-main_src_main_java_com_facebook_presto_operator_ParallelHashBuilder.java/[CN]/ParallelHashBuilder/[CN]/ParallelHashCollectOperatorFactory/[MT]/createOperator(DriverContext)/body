{
  checkState(!closed,"Factory is already closed");
  OperatorContext operatorContext=driverContext.addOperatorContext(operatorId,ParallelHashBuilder.class.getSimpleName());
  return new ParallelHashCollectOperator(operatorContext,partitionFutures,types,hashChannels,hashChannel,expectedPositions);
}
