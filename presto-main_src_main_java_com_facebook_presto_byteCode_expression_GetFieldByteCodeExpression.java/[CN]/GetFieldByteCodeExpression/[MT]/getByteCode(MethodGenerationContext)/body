{
  if (instance == null) {
    return getStaticInstruction(declaringClass,name,getType());
  }
  return new Block().append(instance.getByteCode(generationContext)).getField(declaringClass,name,getType());
}
