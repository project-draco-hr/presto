{
  if (!(range.getEnd() > position)) {
    return false;
  }
  if (position < 0) {
    position=range.getStart();
    offset=0;
  }
 else {
    position++;
    offset+=SIZE_OF_DOUBLE;
  }
  return true;
}
