{
  KeyValuePairs pairs=state.get();
  if (pairs == null) {
    out.appendNull();
  }
 else {
    Slice slice=pairs.serialize();
    out.writeBytes(slice,0,slice.length());
    out.closeEntry();
  }
}
