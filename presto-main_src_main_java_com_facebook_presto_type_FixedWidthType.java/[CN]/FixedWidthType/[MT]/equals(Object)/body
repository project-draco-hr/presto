{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  FixedWidthType type=(FixedWidthType)o;
  if (!this.type.equals(type.type)) {
    return false;
  }
  return true;
}
