{
synchronized (this) {
    if (!anotherUpdateRequested) {
      return;
    }
  }
synchronized (HttpRemoteTask.this) {
    if (currentRequest != this) {
      return;
    }
  }
  updateState(false);
}
