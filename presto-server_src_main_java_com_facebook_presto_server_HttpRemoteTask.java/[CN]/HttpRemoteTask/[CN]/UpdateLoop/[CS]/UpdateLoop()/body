{
  request=prepareGet().setUri(taskInfo.get().getSelf()).setHeader(HttpHeaders.CONTENT_TYPE,MediaType.JSON_UTF_8.toString()).setHeader(PrestoHeaders.PRESTO_CURRENT_STATE,taskInfo.get().getState().toString()).setHeader(PrestoHeaders.PRESTO_MAX_WAIT,"200ms").build();
}
