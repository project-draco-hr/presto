{
  URI build=uriBuilderFrom(taskInfo.get().getSelf()).addParameter("waitForStateChange","200ms").build();
  request=prepareGet().setUri(build).setHeader(HttpHeaders.CONTENT_TYPE,MediaType.JSON_UTF_8.toString()).build();
}
