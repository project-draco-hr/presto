{
  if (taskInfo.getState().isDone()) {
    return;
  }
  if (newValue.getVersion() < taskInfo.getVersion()) {
    return;
  }
  taskInfo=newValue;
  if (newValue.getState().isDone()) {
    pendingSplits.clear();
  }
}
