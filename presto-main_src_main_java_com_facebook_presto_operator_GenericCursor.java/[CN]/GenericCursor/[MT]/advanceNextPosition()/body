{
  if (blockCursor == null) {
    return FINISHED;
  }
  hasAdvanced=true;
  AdvanceResult result=blockCursor.advanceNextPosition();
  if (result != FINISHED) {
    return result;
  }
  return advanceNextValue();
}
