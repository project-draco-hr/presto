{
  long sampleWeight;
  if (masks != null) {
    sampleWeight=masks.getBoolean() ? 1 : 0;
  }
 else {
    sampleWeight=sampleWeights != null ? sampleWeights.getLong() : 1;
  }
  return sampleWeight;
}
