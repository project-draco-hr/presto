{
  return new InMemoryWindowOperatorIterator(source,orderingChannel,tupleInfos,outputChannels,windowFunctions,partitionFields,sortFields,sortOrder,expectedPositions,taskMemoryManager,operatorStats);
}
