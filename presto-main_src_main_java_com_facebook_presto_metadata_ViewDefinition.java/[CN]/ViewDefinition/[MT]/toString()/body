{
  return toStringHelper(this).add("originalSql",originalSql).add("catalog",catalog.orElse(null)).add("schema",schema.orElse(null)).add("columns",columns).add("owner",owner).omitNullValues().toString();
}
