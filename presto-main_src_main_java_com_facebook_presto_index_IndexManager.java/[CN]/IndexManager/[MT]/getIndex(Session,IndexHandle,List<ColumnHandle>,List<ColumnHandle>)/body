{
  ConnectorSession connectorSession=session.toConnectorSession(indexHandle.getConnectorId());
  return getResolver(indexHandle).getIndex(connectorSession,indexHandle.getConnectorHandle(),lookupSchema,outputSchema);
}
