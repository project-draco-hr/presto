{
  return Iterators.peekingIterator(Iterators.transform(sourceValueBlock.pairIterator(),new Function<Pair,Pair>(){
    @Override public Pair apply(    Pair input){
      return new Pair(input.getPosition(),translateValue(input.getValue()));
    }
  }
));
}
