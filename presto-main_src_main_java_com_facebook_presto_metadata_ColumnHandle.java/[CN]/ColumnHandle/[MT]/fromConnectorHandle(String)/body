{
  return new Function<ConnectorColumnHandle,ColumnHandle>(){
    @Override public ColumnHandle apply(    ConnectorColumnHandle handle){
      return new ColumnHandle(connectorId,handle);
    }
  }
;
}
