{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  final NativePartitionKey other=(NativePartitionKey)obj;
  return Objects.equal(this.partitionName,other.partitionName) && Objects.equal(this.name,other.name) && Objects.equal(this.type,other.type)&& Objects.equal(this.value,other.value);
}
