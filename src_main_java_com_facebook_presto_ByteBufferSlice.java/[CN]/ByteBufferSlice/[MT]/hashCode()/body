{
  if (hash != 0) {
    return hash;
  }
  int result=1;
  for (int i=0; i < length(); i++) {
    result=(31 * result) + buffer.get(i);
  }
  if (result == 0) {
    result=1;
  }
  hash=result;
  return hash;
}
