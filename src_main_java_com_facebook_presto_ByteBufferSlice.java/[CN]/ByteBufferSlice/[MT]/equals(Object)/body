{
  if (this == o) {
    return true;
  }
  if (!(o instanceof Slice)) {
    return false;
  }
  Slice slice=(Slice)o;
  if (length() != slice.length()) {
    return false;
  }
  return buffer.equals(slice.toByteBuffer());
}
