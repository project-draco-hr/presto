{
synchronized (pageBuffer) {
    if (isDone()) {
      return;
    }
    bufferState=BufferState.FINISHED;
    sourceCount=0;
    pageBuffer.clear();
    notEmpty.release();
    notFull.release();
  }
}
