{
synchronized (pageBuffer) {
    if (isDone()) {
      return;
    }
    sourceCount--;
    if (sourceCount == 0) {
      if (pageBuffer.isEmpty()) {
        state=State.FINISHED;
      }
      notEmpty.release();
      notFull.release();
    }
  }
}
