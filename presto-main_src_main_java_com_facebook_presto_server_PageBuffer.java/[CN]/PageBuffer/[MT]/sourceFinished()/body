{
synchronized (pageBuffer) {
    if (isDone()) {
      return;
    }
    sourceCount--;
    if (sourceCount == 0) {
      if (pageBuffer.isEmpty()) {
        bufferState=BufferState.FINISHED;
      }
      notEmpty.release();
      notFull.release();
    }
  }
}
