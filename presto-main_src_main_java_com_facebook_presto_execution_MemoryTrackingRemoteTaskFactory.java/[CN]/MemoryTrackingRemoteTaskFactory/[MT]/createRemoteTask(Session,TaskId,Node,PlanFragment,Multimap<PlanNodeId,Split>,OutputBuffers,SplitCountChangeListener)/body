{
  RemoteTask task=remoteTaskFactory.createRemoteTask(session,taskId,node,fragment,initialSplits,outputBuffers,splitCountChangeListener);
  task.addStateChangeListener(new UpdatePeakMemory(stateMachine));
  return task;
}
