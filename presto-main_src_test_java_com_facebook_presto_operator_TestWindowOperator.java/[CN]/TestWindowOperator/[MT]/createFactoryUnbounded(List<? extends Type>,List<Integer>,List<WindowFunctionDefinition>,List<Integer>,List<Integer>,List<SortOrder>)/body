{
  return new WindowOperatorFactory(0,sourceTypes,outputChannels,functions,partitionChannels,sortChannels,sortOrder,new FrameInfo(RANGE,UNBOUNDED_PRECEDING,Optional.empty(),UNBOUNDED_FOLLOWING,Optional.empty()),10);
}
