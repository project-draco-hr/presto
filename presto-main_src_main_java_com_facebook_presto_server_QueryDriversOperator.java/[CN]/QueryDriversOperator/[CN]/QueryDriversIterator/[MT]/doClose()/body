{
  queryState.finish();
  for (  QueryDriver queryDriver : queryDrivers) {
    try {
      queryDriver.abort();
    }
 catch (    Exception e) {
      log.warn(e,"Error canceling query driver");
    }
  }
}
