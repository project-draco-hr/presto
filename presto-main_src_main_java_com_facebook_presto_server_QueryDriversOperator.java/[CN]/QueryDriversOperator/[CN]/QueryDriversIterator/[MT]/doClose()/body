{
  queryState.finish();
  for (  QueryDriver queryDriver : queryDrivers) {
    try {
      queryDriver.cancel();
    }
 catch (    Exception e) {
      log.warn("Error canceling query driver",e);
    }
  }
}
