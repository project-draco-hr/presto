{
  checkState(lastTuple != null,"nothing processed yet");
  return new BlockBuilder(0,RESULT_TUPLE_INFO).append(rowCount).append(runsCount + 1).append(minPosition).append(maxPosition).append(rowCount / (runsCount + 1)).build();
}
