{
  this(taskInstanceId,new StateMachine<>(requireNonNull(taskId,"taskId is null") + "-buffer",requireNonNull(executor,"executor is null"),OPEN,TERMINAL_BUFFER_STATES),maxBufferSize,systemMemoryUsageListener,executor);
}
