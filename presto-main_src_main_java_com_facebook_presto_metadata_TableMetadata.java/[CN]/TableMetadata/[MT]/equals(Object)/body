{
  if (this == obj) {
    return true;
  }
  if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  TableMetadata o=(TableMetadata)obj;
  return Objects.equal(catalogName,o.catalogName) && Objects.equal(schemaName,o.schemaName) && Objects.equal(tableName,o.tableName)&& Objects.equal(columns,o.columns);
}
