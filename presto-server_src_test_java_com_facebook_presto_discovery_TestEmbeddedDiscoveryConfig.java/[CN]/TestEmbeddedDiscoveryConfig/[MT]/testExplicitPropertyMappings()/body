{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("discovery-server.enabled","true").build();
  EmbeddedDiscoveryConfig expected=new EmbeddedDiscoveryConfig().setEnabled(true);
  assertFullMapping(properties,expected);
}
