{
  if (this == o) {
    return true;
  }
  if (!(o instanceof Slice)) {
    return false;
  }
  if (o instanceof ByteArraySlice) {
    return equals((ByteArraySlice)o);
  }
  Slice slice=(Slice)o;
  return (length == slice.length()) && Arrays.equals(getBytes(),slice.getBytes());
}
