{
  return new TupleStreamDeserializer(){
    @Override public TupleStream deserialize(    Range totalRange,    Slice slice){
      throw new UnsupportedOperationException();
    }
    @Override public Blocks deserializeBlocks(    final Range totalRange,    final Slice slice){
      return UncompressedSerde.deserializeBlocks(totalRange,slice);
    }
  }
;
}
