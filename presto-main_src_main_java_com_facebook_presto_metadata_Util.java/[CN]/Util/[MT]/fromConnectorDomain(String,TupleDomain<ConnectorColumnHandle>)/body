{
  return domain.transform(new TupleDomain.Function<ConnectorColumnHandle,ColumnHandle>(){
    @Override public ColumnHandle apply(    ConnectorColumnHandle input){
      return new ColumnHandle(connectorId,input);
    }
  }
);
}
