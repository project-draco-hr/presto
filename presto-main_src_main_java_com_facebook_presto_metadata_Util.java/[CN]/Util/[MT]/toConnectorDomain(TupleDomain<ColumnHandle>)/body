{
  return domain.transform(new TupleDomain.Function<ColumnHandle,ConnectorColumnHandle>(){
    @Override public ConnectorColumnHandle apply(    ColumnHandle input){
      return input.getConnectorHandle();
    }
  }
);
}
