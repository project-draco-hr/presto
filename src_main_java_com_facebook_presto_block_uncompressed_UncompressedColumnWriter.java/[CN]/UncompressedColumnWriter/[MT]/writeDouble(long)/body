{
  while (cursor.advanceNextPosition()) {
    buffer.appendDouble(cursor.getDouble(index));
    tupleCount++;
    flushIfNecessary();
    if (cursor.getPosition() >= end) {
      return true;
    }
  }
  return false;
}
