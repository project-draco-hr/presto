{
  return new ClientSession(coordinator.getBaseUrl(),SESSION.getUser(),SESSION.getSource(),SESSION.getCatalog(),SESSION.getSchema(),SESSION.getTimeZoneKey().getTimeZoneId(),SESSION.getLocale(),true);
}
