{
  assertQuery("CREATE MATERIALIZED VIEW test_mview_orders AS SELECT * FROM " + format("%s.%s.orders",TPCH_CATALOG_NAME,TPCH_SCHEMA_NAME),"SELECT count(*) FROM orders");
  MILLISECONDS.sleep(500);
  assertQuery("SELECT * FROM test_mview_orders","SELECT * FROM orders");
}
