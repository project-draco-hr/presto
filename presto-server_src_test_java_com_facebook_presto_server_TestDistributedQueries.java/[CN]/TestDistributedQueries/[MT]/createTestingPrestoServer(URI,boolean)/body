{
  Map<String,String> properties=ImmutableMap.<String,String>builder().put("query.client.timeout","10m").put("exchange.http-client.read-timeout","1h").put("datasources","native,tpch,tpch_sampled").build();
  return new TestingPrestoServer(coordinator,properties,ENVIRONMENT,discoveryUri);
}
