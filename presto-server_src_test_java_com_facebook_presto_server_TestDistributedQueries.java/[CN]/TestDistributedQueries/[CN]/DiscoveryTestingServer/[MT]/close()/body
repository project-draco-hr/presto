{
  try {
    if (server != null) {
      try {
        server.stop();
      }
 catch (      Exception e) {
        Throwables.propagate(e);
      }
    }
  }
  finally {
    FileUtils.deleteRecursively(tempDir);
  }
}
