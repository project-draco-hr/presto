{
  assertEquals(getBufferInfo(sharedBuffer,queueId),new BufferInfo(queueId,false,bufferedPages,pagesSent,new PageBufferInfo(partition,pageBufferBufferedPages,retainedSizeOfPages(pageBufferBufferedPages).toBytes(),pageBufferPagesSent,pageBufferPagesSent)));
}
