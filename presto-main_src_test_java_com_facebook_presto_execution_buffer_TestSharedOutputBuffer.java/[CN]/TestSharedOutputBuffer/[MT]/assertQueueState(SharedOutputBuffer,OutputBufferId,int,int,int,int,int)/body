{
  assertEquals(getBufferInfo(sharedBuffer,bufferId),new BufferInfo(bufferId,false,bufferedPages,pagesSent,new PageBufferInfo(partition,pageBufferBufferedPages,retainedSizeOfPages(pageBufferBufferedPages).toBytes(),pageBufferPagesSent,pageBufferPagesSent)));
}
