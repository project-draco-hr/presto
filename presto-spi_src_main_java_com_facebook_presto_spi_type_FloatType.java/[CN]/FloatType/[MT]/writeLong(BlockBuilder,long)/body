{
  try {
    Math.toIntExact(value);
  }
 catch (  ArithmeticException e) {
    throw new PrestoException(INTERNAL_ERROR,String.format("Value (%sb) is not a valid single-precision float",value,Long.toBinaryString(value).replace(' ','0')));
  }
  blockBuilder.writeInt((int)value).closeEntry();
}
