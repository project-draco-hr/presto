{
  return new Function<NamedSlot,QualifiedName>(){
    @Override public QualifiedName apply(    NamedSlot input){
      return QualifiedName.of(input.getPrefix().get(),input.getAttribute().get());
    }
  }
;
}
