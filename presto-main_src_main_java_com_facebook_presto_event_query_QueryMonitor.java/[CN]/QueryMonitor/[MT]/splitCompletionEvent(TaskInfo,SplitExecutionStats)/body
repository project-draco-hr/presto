{
  try {
    eventClient.post(new SplitCompletionEvent(taskInfo.getQueryId(),taskInfo.getStageId(),taskInfo.getTaskId(),splitExecutionStats.getExecutionStartTime(),splitExecutionStats.getTimeToFirstByte(),splitExecutionStats.getTimeToLastByte(),splitExecutionStats.getCompletedDataSize(),splitExecutionStats.getCompletedPositions(),splitExecutionStats.getWall(),splitExecutionStats.getCpu(),splitExecutionStats.getUser(),objectMapper.writeValueAsString(splitExecutionStats.getSplitInfo())));
  }
 catch (  JsonProcessingException e) {
    throw Throwables.propagate(e);
  }
}
