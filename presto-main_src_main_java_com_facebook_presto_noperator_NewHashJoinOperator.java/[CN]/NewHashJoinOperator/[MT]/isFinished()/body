{
  boolean finished=finishing && cursors[0] == null && pageBuilder.isEmpty();
  if (finished) {
    hash=null;
    for (int i=0; i < cursors.length; i++) {
      cursors[i]=null;
    }
    pageBuilder.reset();
  }
  return finished;
}
