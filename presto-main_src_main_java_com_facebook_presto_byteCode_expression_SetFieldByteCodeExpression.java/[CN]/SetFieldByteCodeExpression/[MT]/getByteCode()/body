{
  if (instance == null) {
    return new Block(null).append(value.getByteCode()).putStaticField(declaringClass,name,fieldType);
  }
  return new Block(null).append(instance.getByteCode()).append(value.getByteCode()).putField(declaringClass,name,fieldType);
}
