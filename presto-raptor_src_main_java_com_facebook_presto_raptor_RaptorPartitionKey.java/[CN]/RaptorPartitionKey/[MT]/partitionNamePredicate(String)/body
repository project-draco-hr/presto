{
  checkNotNull(partitionName,"partitionName is null");
  return new Predicate<RaptorPartitionKey>(){
    @Override public boolean apply(    RaptorPartitionKey input){
      return partitionName.equals(input.getPartitionName());
    }
  }
;
}
