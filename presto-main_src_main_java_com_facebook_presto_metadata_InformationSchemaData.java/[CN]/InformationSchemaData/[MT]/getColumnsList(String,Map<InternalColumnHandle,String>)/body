{
  return metadata.listTableColumns(QualifiedTablePrefix.builder(catalogName).schemaName(getFilterColumn(filters,TABLE_COLUMNS,"table_schema")).tableName(getFilterColumn(filters,TABLE_COLUMNS,"table_name")).build());
}
