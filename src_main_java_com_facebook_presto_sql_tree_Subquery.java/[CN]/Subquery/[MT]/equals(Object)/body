{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Subquery subquery=(Subquery)o;
  if (!query.equals(subquery.query)) {
    return false;
  }
  return true;
}
