{
  this.operatorContext=checkNotNull(operatorContext,"operatorContext is null");
  this.setSupplier=checkNotNull(setSupplier,"setProvider is null");
  this.setChannel=setChannel;
  this.channelSetBuilder=new NewChannelSetBuilder(setSupplier.getTupleInfo(),expectedPositions,checkNotNull(operatorContext,"operatorContext is null"));
}
