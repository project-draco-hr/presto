{
  List<Slice> slices=Collections.EMPTY_LIST;
  dictionarySerde.serialize(slices,sliceOutput);
  Assert.assertTrue(Iterables.elementsEqual(slices,DictionarySerde.deserialize(sliceOutput.slice().input())));
}
