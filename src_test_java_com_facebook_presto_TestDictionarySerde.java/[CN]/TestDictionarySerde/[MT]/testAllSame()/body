{
  BlockStream blockStream=Blocks.createBlockStream(0,"a","a","a","a","a","a","a");
  dictionarySerde.serialize(blockStream,sliceOutput);
  Assert.assertTrue(Iterables.elementsEqual(Iterables.concat(blockStream),Iterables.concat(dictionarySerde.deserialize(sliceOutput.slice()))));
}
