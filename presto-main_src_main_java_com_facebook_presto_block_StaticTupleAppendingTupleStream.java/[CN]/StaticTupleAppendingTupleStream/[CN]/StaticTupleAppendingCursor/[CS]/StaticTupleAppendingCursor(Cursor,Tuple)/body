{
  super(checkNotNull(cursor,"cursor is null"));
  this.tuple=checkNotNull(tuple,"tuple is null");
  tupleInfo=new TupleInfo(ImmutableList.<TupleInfo.Type>builder().addAll(cursor.getTupleInfo().getTypes()).addAll(tuple.getTupleInfo().getTypes()).build());
}
