{
  assertEquals(buffer.getInfo(),new BufferInfo(BUFFER_ID,false,bufferedPages,pagesSent,new PageBufferInfo(BUFFER_ID.getId(),bufferedPages,sizeOfPages(bufferedPages).toBytes(),bufferedPages + pagesSent,bufferedPages + pagesSent)));
  assertEquals(buffer.isDestroyed(),false);
}
