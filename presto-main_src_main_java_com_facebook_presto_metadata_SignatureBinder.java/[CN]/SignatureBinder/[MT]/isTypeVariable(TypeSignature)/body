{
  if (typeVariableConstraints.containsKey(signature.getBase())) {
    checkState(signature.getParameters().isEmpty(),"TypeSignature that represent type variable shouldn't be parametrized");
    return true;
  }
  return false;
}
