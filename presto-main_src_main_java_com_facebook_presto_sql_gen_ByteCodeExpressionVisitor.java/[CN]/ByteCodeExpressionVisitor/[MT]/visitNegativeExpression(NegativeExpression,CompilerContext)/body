{
  ByteCodeNode value=process(node.getValue(),context);
  FunctionBinding functionBinding=bootstrapFunctionBinder.bindOperator(OperatorType.NEGATION,getSessionByteCode,ImmutableList.of(value),types(node.getValue()));
  return visitFunctionBinding(context,functionBinding,node.toString());
}
