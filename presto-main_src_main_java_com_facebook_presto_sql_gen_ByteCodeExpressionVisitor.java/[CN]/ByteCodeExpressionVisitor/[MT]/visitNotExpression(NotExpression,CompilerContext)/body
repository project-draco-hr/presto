{
  ByteCodeNode value=process(node.getValue(),context);
  return new Block(context).comment(node.toString()).append(value).invokeStatic(CompilerOperations.class,"not",boolean.class,boolean.class);
}
