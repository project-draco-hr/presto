{
  return new Predicate<ColumnHandle>(){
    @Override public boolean apply(    ColumnHandle columnHandle){
      return metadata.getColumnMetadata(tableHandle,columnHandle).isPartitionKey();
    }
  }
;
}
