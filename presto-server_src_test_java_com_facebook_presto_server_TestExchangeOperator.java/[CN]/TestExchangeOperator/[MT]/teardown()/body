{
  for (  LifeCycleManager lifeCycleManager : lifeCycleManagers) {
    lifeCycleManager.stop();
  }
  if (httpClient != null) {
    httpClient.close();
  }
  if (executor != null) {
    executor.shutdownNow();
    executor=null;
  }
}
