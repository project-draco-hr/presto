{
  Block block=BIGINT.createBlockBuilder(new BlockBuilderStatus()).appendNull().appendLong(42).appendNull().appendLong(42).build();
  assertTrue(block.isNull(0));
  assertEquals(block.getLong(1),42L);
  assertTrue(block.isNull(2));
  assertEquals(block.getLong(3),42L);
}
