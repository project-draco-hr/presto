{
  checkNotNull(prefix,"prefix is null");
  ImmutableList.Builder<QualifiedTableName> builder=ImmutableList.builder();
  for (  QualifiedTableName tableName : tables.keySet()) {
    if (prefix.getCatalogName().equals(tableName.getCatalogName()) && prefix.getSchemaName().isPresent() || prefix.getSchemaName().get().equals(tableName.getSchemaName()) && prefix.getTableName().isPresent() || prefix.getTableName().get().equals(tableName.getTableName())) {
      builder.add(tableName);
    }
  }
  return builder.build();
}
