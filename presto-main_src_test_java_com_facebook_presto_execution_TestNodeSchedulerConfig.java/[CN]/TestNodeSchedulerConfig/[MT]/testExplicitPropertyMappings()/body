{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("node-scheduler.min-candidates","1").put("node-scheduler.max-splits-per-node","300").build();
  NodeSchedulerConfig expected=new NodeSchedulerConfig().setMinCandidates(1).setMaxSplitsPerNode(300);
  ConfigAssertions.assertFullMapping(properties,expected);
}
