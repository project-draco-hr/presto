{
  int markerWidth=3;
  int range=width - markerWidth;
  int lower=tick % range;
  if ((tick / range) % 2 == 1) {
    lower=range - lower;
  }
  return Strings.repeat(" ",lower) + "<" + Strings.repeat("=",markerWidth - 2)+ ">"+ Strings.repeat(" ",width - (lower + markerWidth));
}
