{
  QueryState state=queryState.get();
  FailureInfo failureInfo=null;
  if (state != FINISHED) {
    failureInfo=toFailure(failureCause);
  }
  return new QueryInfo(queryId,session,state,self,outputFieldNames,query,queryStats,stageInfo,failureInfo);
}
