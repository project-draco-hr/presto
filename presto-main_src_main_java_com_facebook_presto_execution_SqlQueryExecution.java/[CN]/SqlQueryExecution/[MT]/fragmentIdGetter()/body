{
  return new Function<StageExecutionPlan,String>(){
    @Override public String apply(    @Nullable StageExecutionPlan input){
      return String.valueOf(input.getFragment().getId());
    }
  }
;
}
