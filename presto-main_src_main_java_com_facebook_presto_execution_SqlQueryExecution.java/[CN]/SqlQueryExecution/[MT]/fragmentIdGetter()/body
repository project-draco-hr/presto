{
  return new Function<StageExecutionPlan,PlanFragmentId>(){
    @Override public PlanFragmentId apply(    @Nullable StageExecutionPlan input){
      return input.getFragment().getId();
    }
  }
;
}
