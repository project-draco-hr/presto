{
  if (line.endsWith(";")) {
    line=line.substring(0,line.length() - 1);
  }
  if (line.equalsIgnoreCase("quit")) {
    return false;
  }
  Execute query=new Execute();
  query.server=server;
  query.query=line;
  query.run();
  return true;
}
