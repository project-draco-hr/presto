{
  state=State.FAILED;
  try {
    next=computeNext();
    if (state != State.DONE) {
      state=State.READY;
      Preconditions.checkNotNull(next,"next page is null");
      return true;
    }
    return false;
  }
  finally {
    if (state != State.READY) {
      doClose();
    }
  }
}
