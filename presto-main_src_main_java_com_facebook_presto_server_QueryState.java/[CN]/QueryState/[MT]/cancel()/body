{
synchronized (pageBuffer) {
    if (state != State.RUNNING) {
      return;
    }
    state=State.CANCELED;
    sourceCount=0;
    pageBuffer.clear();
    notEmpty.release();
    notFull.release();
  }
}
