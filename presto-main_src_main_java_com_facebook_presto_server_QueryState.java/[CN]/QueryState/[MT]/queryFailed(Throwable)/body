{
synchronized (pageBuffer) {
    if (isDone()) {
      causes.add(cause);
      return;
    }
    state=State.FAILED;
    causes.add(cause);
    sourceCount=0;
    pageBuffer.clear();
    notEmpty.release();
    notFull.release();
  }
}
