{
  Map<String,String> properties=new ImmutableMap.Builder<String,String>().put("storage-manager.data-directory","/data").build();
  StorageManagerConfig expected=new StorageManagerConfig().setDataDirectory(new File("/data"));
  ConfigAssertions.assertFullMapping(properties,expected);
}
