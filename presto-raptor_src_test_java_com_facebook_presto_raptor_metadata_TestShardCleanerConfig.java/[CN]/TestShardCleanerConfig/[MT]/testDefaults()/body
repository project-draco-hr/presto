{
  assertRecordedDefaults(recordDefaults(ShardCleanerConfig.class).setMaxTransactionAge(new Duration(1,DAYS)).setTransactionCleanerInterval(new Duration(10,MINUTES)).setLocalCleanerInterval(new Duration(1,HOURS)).setLocalCleanTime(new Duration(4,HOURS)).setBackupCleanerInterval(new Duration(5,MINUTES)).setBackupCleanTime(new Duration(1,DAYS)).setBackupDeletionThreads(50));
}
