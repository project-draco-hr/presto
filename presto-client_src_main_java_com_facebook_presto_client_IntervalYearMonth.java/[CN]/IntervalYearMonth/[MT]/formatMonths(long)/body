{
  if (months == Long.MIN_VALUE) {
    return "-768614336404564650-8";
  }
  String sign="";
  if (months < 0) {
    sign="-";
    months=-months;
  }
  return format("%s%d-%d",sign,months / 12,months % 12);
}
