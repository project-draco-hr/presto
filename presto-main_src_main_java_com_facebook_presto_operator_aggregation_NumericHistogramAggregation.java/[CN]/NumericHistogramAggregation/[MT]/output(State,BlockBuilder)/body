{
  if (state.get() == null) {
    out.appendNull();
  }
 else {
    Slice slice=MapType.toStackRepresentation(state.get().getBuckets());
    out.writeBytes(slice,0,slice.length());
    out.closeEntry();
  }
}
