{
  return new Function<ConnectorColumnHandle,Long>(){
    @Override public Long apply(    ConnectorColumnHandle handle){
      return checkType(handle,RaptorColumnHandle.class,"columnHandle").getColumnId();
    }
  }
;
}
