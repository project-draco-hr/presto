{
  return new Function<ConnectorColumnHandle,RaptorColumnHandle>(){
    @Override public RaptorColumnHandle apply(    ConnectorColumnHandle handle){
      return checkType(handle,RaptorColumnHandle.class,"columnHandle");
    }
  }
;
}
