{
  HashData hashData=new HashData(checkNotNull(channelHash,"channelHash is null"),checkNotNull(pagesIndex,"pagesIndex is null"));
  boolean wasSet=hashFuture.set(hashData);
  checkState(wasSet,"Hash already set");
}
