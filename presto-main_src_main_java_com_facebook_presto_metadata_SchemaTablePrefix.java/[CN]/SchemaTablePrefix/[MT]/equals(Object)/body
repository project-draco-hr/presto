{
  if (obj == this) {
    return true;
  }
  if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  SchemaTablePrefix o=(SchemaTablePrefix)obj;
  return Objects.equal(schemaName,o.schemaName) && Objects.equal(tableName,o.tableName);
}
