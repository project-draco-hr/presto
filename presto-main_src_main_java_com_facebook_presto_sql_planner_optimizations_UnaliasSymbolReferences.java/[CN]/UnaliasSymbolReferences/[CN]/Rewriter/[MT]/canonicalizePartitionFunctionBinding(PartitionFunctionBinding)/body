{
  return new PartitionFunctionBinding(function.getPartitioningHandle(),canonicalize(function.getOutputLayout()),canonicalize(function.getPartitioningColumns()),canonicalize(function.getHashColumn()),function.isReplicateNulls(),function.getBucketToPartition());
}
