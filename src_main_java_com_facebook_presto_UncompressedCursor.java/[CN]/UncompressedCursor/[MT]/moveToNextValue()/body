{
  if (iterator.hasNext()) {
    nextBlock=iterator.peek();
    nextBlockIndex=0;
    nextOffset=0;
    nextSize=info.size(nextBlock.getSlice(),nextOffset);
  }
 else {
    nextBlock=null;
    nextBlockIndex=-1;
    nextOffset=-1;
    nextSize=-1;
  }
}
