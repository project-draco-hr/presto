{
  for (  FunctionInfo function : functions) {
    checkArgument(this.functions.get(function.getHandle()) == null,"Function already registered: %s",function.getHandle());
  }
  this.functions=new FunctionMap(this.functions,functions,operators);
}
