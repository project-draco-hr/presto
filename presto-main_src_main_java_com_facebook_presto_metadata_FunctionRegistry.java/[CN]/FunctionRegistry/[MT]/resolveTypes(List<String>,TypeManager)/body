{
  return FluentIterable.from(typeNames).transform(new Function<String,Type>(){
    @Override public Type apply(    String type){
      return checkNotNull(typeManager.getType(parseTypeSignature(type)),"Type '%s' not found",type);
    }
  }
).toList();
}
