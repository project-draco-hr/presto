{
  if (closed) {
    return;
  }
  closed=true;
  try (Connection connection=this.connection;Statement statement=this.statement;ResultSet resultSet=this.resultSet){
  }
 catch (  SQLException e) {
    throw Throwables.propagate(e);
  }
}
