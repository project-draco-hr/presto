{
  Cursor cursor=createCursor();
  assertTrue(cursor.advanceNextValue());
  assertTrue(cursor.advanceNextPosition());
  assertTrue(cursor.advanceNextValue());
  assertTrue(cursor.advanceNextPosition());
}
