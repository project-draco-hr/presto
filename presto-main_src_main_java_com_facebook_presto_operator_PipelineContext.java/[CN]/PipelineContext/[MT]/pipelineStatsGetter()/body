{
  return new Function<PipelineContext,PipelineStats>(){
    @Override public PipelineStats apply(    PipelineContext pipelineContext){
      return pipelineContext.getPipelineStats();
    }
  }
;
}
