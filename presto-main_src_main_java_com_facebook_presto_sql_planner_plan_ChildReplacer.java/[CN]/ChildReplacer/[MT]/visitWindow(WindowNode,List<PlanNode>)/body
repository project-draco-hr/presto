{
  return new WindowNode(node.getId(),Iterables.getOnlyElement(newChildren),node.getSpecification(),node.getWindowFunctions(),node.getHashSymbol(),node.getPrePartitionedInputs(),node.getPreSortedOrderPrefix());
}
