{
  while (!operatorStats.isDone() && !driver.isFinished()) {
    driver.process();
    Page output=outputOperator.getOutput();
    if (output != null) {
      return output;
    }
  }
  return endOfData();
}
