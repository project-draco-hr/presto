{
  while (true) {
    QueryInfo currentQueryInfo=queryInfo.get();
    QueryInfo newQueryInfo=currentQueryInfo.queryState(queryExecutionState.getQueryState());
    if (queryInfo.compareAndSet(currentQueryInfo,newQueryInfo)) {
      return newQueryInfo;
    }
  }
}
