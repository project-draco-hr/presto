{
synchronized (this) {
    if (noMoreBufferIds) {
      return;
    }
    noMoreBufferIds=true;
  }
  OutputBuffers outputBuffers=INITIAL_EMPTY_OUTPUT_BUFFERS.withBuffers(partitions).withNoMoreBufferIds();
  outputBufferTarget.accept(outputBuffers);
}
