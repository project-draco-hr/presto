{
  printNode(node,"RowNumberLimit",format("partition by = %s|row_number limit %s",Joiner.on(", ").join(node.getPartitionBy()),node.getMaxRowCountPerPartition()),NODE_COLORS.get(NodeType.WINDOW));
  return node.getSource().accept(this,context);
}
