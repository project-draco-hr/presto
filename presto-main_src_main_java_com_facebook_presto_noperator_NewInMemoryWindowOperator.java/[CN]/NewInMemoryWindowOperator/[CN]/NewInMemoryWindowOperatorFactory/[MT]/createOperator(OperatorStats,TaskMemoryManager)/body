{
  checkState(!closed,"Factory is already closed");
  return new NewInMemoryWindowOperator(sourceTupleInfos,orderingChannel,outputChannels,windowFunctions,partitionFields,sortFields,sortOrder,expectedPositions,taskMemoryManager);
}
