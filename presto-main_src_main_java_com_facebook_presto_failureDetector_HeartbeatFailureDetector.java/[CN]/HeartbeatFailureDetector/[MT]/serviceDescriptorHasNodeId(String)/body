{
  checkNotNull(nodeId,"nodeId is null");
  return new Predicate<ServiceDescriptor>(){
    @Override public boolean apply(    ServiceDescriptor descriptor){
      return nodeId.equals(descriptor.getNodeId());
    }
  }
;
}
