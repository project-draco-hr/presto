{
  try {
    if (!stateMachine.starting()) {
      return;
    }
    stateMachine.getStats().recordExecutionStart();
    dropAlias();
    stateMachine.finished();
  }
 catch (  Exception e) {
    fail(e);
  }
}
