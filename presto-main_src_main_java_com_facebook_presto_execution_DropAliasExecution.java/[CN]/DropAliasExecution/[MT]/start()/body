{
  try {
    if (!stateMachine.starting()) {
      return;
    }
    stateMachine.recordExecutionStart();
    dropAlias();
    stateMachine.finished();
  }
 catch (  Exception e) {
    fail(e);
  }
}
