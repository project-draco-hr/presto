{
  return ImmutableList.copyOf(transform(NODES_TABLE.getColumns(),new Function<ColumnMetadata,ColumnType>(){
    @Override public ColumnType apply(    ColumnMetadata columnMetadata){
      return columnMetadata.getType();
    }
  }
));
}
