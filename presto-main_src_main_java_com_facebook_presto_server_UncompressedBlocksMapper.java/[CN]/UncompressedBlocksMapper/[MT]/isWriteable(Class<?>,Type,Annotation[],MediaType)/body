{
  return List.class.isAssignableFrom(type) && TypeToken.of(genericType).resolveType(LIST_GENERIC_TOKEN).getRawType().equals(UncompressedBlock.class) && mediaType.isCompatible(PRESTO_BLOCKS_TYPE);
}
