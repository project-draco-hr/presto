{
  return new Function<byte[],String>(){
    @Nullable @Override public String apply(    @Nullable byte[] input){
      if (input == null) {
        return null;
      }
      return BaseEncoding.base64().encode(input);
    }
  }
;
}
