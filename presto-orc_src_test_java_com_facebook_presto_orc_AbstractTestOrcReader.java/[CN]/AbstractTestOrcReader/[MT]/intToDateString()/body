{
  return new Function<Integer,String>(){
    @Nullable @Override public String apply(    @Nullable Integer input){
      if (input == null) {
        return null;
      }
      return ISODateTimeFormat.date().withZoneUTC().print(TimeUnit.DAYS.toMillis(input));
    }
  }
;
}
