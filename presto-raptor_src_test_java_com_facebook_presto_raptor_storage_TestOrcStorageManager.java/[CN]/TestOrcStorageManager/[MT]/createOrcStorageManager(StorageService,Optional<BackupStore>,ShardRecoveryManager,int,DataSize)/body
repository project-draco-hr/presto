{
  return new OrcStorageManager(CURRENT_NODE,storageService,backupStore,SHARD_DELTA_CODEC,ORC_MAX_MERGE_DISTANCE,ORC_MAX_READ_SIZE,ORC_STREAM_BUFFER_SIZE,new BackupManager(backupStore,1),recoveryManager,new TypeRegistry(),CONNECTOR_ID,DELETION_THREADS,SHARD_RECOVERY_TIMEOUT,maxShardRows,maxFileSize);
}
