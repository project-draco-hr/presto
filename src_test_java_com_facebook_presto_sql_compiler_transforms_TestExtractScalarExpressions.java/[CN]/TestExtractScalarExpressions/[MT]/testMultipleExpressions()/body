{
  assertRewrite("SELECT SUM(ORDERS.orderkey + ORDERS.custkey) x, AVG(ORDERS.orderkey * 2) y FROM ORDERS","SELECT SUM(_R0._a0) x, AVG(_R0._a1) y\n" + "FROM (\n" + "   SELECT (ORDERS.orderkey + ORDERS.custkey) _a0, (ORDERS.orderkey * 2) _a1\n"+ "   FROM ORDERS\n"+ ") _R0");
}
