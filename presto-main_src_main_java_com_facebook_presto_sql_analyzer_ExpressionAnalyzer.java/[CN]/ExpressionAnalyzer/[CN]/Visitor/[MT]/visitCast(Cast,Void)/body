{
  process(node.getExpression(),context);
switch (node.getType()) {
case "BOOLEAN":
    return Type.BOOLEAN;
case "DOUBLE":
  return Type.DOUBLE;
case "BIGINT":
return Type.LONG;
case "VARCHAR":
return Type.STRING;
}
throw new SemanticException(node,"Cannot cast to type: " + node.getType());
}
