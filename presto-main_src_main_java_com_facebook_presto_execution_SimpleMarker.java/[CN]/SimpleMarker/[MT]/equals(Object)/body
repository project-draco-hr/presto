{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  SimpleMarker that=(SimpleMarker)o;
  return Objects.equal(this.inclusive,that.inclusive) && Objects.equal(this.value,that.value) && Objects.equal(this.type,that.type);
}
