{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ValueBuffer that=(ValueBuffer)o;
  return Objects.equals(columnIndex,that.columnIndex) && Objects.equals(type,that.type) && Objects.equals(value,that.value);
}
