{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  final FunctionInfo other=(FunctionInfo)obj;
  return Objects.equal(this.signature,other.signature) && Objects.equal(this.isAggregate,other.isAggregate) && Objects.equal(this.isWindow,other.isWindow);
}
