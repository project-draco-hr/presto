{
  if (this == obj) {
    return true;
  }
  if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  FunctionInfo o=(FunctionInfo)obj;
  return Objects.equal(isWindow,o.isWindow) && Objects.equal(isAggregate,o.isAggregate) && Objects.equal(name,o.name)&& Objects.equal(argumentTypes,o.argumentTypes)&& Objects.equal(returnType,o.returnType);
}
