{
  return new Supplier<Map<String,Map<String,ExampleTable>>>(){
    @Override public Map<String,Map<String,ExampleTable>> get(){
      try {
        return lookupSchemas(metadataUri,catalogCodec);
      }
 catch (      IOException e) {
        throw Throwables.propagate(e);
      }
    }
  }
;
}
