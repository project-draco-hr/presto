{
  int length=0;
  double sum=0;
  for (  Number value : values) {
    if (value == null) {
      continue;
    }
    length++;
    sum+=value.doubleValue();
  }
  if (length == 0) {
    return null;
  }
  return sum / length;
}
