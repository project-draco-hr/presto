{
  if (cursor.isNull(0)) {
    return currentMax;
  }
  Slice value=cursor.getSlice(0);
  if (currentMax == null) {
    return value;
  }
 else {
    return Ordering.natural().max(currentMax,value);
  }
}
