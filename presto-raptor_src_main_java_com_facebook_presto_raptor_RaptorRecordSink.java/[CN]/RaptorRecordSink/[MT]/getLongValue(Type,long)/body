{
  if (type.equals(BIGINT) || type.equals(TIME) || type.equals(TIMESTAMP)|| type.equals(INTERVAL_YEAR_MONTH)|| type.equals(INTERVAL_DAY_TIME)) {
    return value;
  }
  if (type.equals(DATE)) {
    return value;
  }
  throw unsupportedType(type);
}
