{
  Tuple tuple=TupleInfo.SINGLE_BOOLEAN.builder().appendNull().build();
  assertTrue(tuple.isNull(0));
  assertEquals(tuple.getBoolean(0),false);
  assertEquals(tuple.size(),SIZE_OF_BYTE + SIZE_OF_BYTE);
}
