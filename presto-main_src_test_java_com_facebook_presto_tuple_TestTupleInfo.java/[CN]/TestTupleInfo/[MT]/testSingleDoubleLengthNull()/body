{
  Tuple tuple=TupleInfo.SINGLE_DOUBLE.builder().appendNull().build();
  assertTrue(tuple.isNull());
  assertEquals(tuple.getDouble(),0.0);
  assertEquals(tuple.size(),SIZE_OF_DOUBLE + SIZE_OF_BYTE);
}
