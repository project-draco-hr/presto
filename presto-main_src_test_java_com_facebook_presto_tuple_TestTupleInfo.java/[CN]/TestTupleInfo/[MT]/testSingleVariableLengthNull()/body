{
  Tuple tuple=TupleInfo.SINGLE_VARBINARY.builder().appendNull().build();
  assertTrue(tuple.isNull());
  assertEquals(tuple.getSlice(),Slices.EMPTY_SLICE);
  assertEquals(tuple.size(),SIZE_OF_INT + SIZE_OF_BYTE);
}
