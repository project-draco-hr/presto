{
  updateFinishedState();
  SharedBufferInfo sharedBufferInfo=sharedBuffer.getInfo();
synchronized (this) {
    return new TaskInfo(queryId,stageId,taskId,nextTaskInfoVersion.getAndIncrement(),getState(),location,sharedBufferInfo,getNoMoreSplits(),stats,toFailures(failureCauses));
  }
}
