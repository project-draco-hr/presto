{
  return Iterators.peekingIterator(Iterators.transform(getPositions().iterator(),new Function<Long,Pair>(){
    @Override public Pair apply(    Long position){
      return new Pair(position,value);
    }
  }
));
}
