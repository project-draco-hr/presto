{
  long rows=0;
  long bytes=0;
  for (  Page page : sink) {
    rows+=page.getPositionCount();
    bytes=page.getDataSize().toBytes();
  }
  handler.finish();
  return new OutputStats(rows,bytes);
}
