{
  return FluentIterable.from(columnTypes).transform(new Function<Type,StorageType>(){
    @Override public StorageType apply(    Type type){
      return toStorageType(type);
    }
  }
).toList();
}
