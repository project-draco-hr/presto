{
  Iterable<PartitionChunk> iterator=client.getPartitionChunks(TABLE,ImmutableList.<String>of(),ImmutableList.<String>of());
  List<PartitionChunk> chunks=ImmutableList.copyOf(iterator);
  assertTrue(chunks.isEmpty());
}
