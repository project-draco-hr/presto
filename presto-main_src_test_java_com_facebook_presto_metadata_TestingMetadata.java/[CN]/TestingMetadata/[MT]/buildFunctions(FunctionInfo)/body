{
  return Multimaps.index(ImmutableList.copyOf(infos),new Function<FunctionInfo,QualifiedName>(){
    @Override public QualifiedName apply(    FunctionInfo input){
      return input.getName();
    }
  }
);
}
