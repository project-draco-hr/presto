{
  MapBinder<String,Class<? extends TableHandle>> tableHandleTypes=MapBinder.newMapBinder(binder,new TypeLiteral<String>(){
  }
,new TypeLiteral<Class<? extends TableHandle>>(){
  }
);
  return tableHandleTypes.addBinding(tableHandlerType);
}
