{
  if (this == obj) {
    return true;
  }
 else   if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  CreateOrReplaceMaterializedView o=(CreateOrReplaceMaterializedView)obj;
  return Objects.equal(name,o.name) && Objects.equal(tableDefinition,o.tableDefinition);
}
