{
  Operator source=createOperator(new Page(createLongsBlock(1,3,5,7)),new Page(createLongsBlock(2,4,6,8)));
  Operator actual=new InMemoryWindowOperator(source,0,ints(0),ROW_NUMBER,ints(),ints(),bools(),10,new TaskMemoryManager(MAX_SIZE));
  Operator expected=createOperator(new Page(createLongsBlock(1,3,5,7,2,4,6,8),createLongsBlock(1,2,3,4,5,6,7,8)));
  assertOperatorEquals(actual,expected);
}
