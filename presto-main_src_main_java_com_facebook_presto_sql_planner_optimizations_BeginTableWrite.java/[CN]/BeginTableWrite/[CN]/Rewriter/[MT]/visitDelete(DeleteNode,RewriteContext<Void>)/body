{
  TableHandle handle=metadata.beginDelete(session,node.getTarget().getHandle());
  return new DeleteNode(node.getId(),node.getSource().accept(this,context),new DeleteHandle(handle),node.getRowId(),node.getOutputSymbols());
}
