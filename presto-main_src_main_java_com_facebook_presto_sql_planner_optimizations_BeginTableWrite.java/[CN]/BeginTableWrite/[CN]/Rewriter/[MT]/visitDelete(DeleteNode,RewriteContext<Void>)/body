{
  TableHandle handle=metadata.beginDelete(session,node.getTarget().getHandle());
  return new DeleteNode(node.getId(),rewriteDeleteTableScan(node.getSource(),handle,context),new DeleteHandle(handle),node.getRowId(),node.getOutputSymbols());
}
