{
  return new TableWriterNode(node.getId(),node.getSource().accept(this,context),createWriterTarget(node.getTarget()),node.getColumns(),node.getColumnNames(),node.getOutputSymbols(),node.getSampleWeightSymbol());
}
