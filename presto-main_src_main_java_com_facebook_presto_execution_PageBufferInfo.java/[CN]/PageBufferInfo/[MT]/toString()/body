{
  return toStringHelper(this).add("partition",partition).add("bufferedPages",bufferedPages).add("queuedPages",queuedPages).add("bufferedBytes",bufferedBytes).add("rowsAdded",rowsAdded).add("pagesAdded",pagesAdded).toString();
}
