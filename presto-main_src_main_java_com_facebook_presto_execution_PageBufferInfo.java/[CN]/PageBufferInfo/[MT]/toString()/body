{
  return toStringHelper(this).add("partition",partition).add("bufferedPages",bufferedPages).add("queuedPages",queuedPages).add("bufferedBytes",bufferedBytes).add("pagesAdded",pagesAdded).toString();
}
